# 測試案例撰寫契約（Cloud-360）

> **這份文件是測試案例格式的唯一真實來源**，不限工具、不限流程。
> 用 Claude Code、Cursor、Antigravity 或純手寫都適用；AI 工具產生的案例
> 一樣要符合這裡的契約。
>
> 這裡定義**必要欄位與模板**。撰寫方法論（怎麼寫才寫得好、判準、完整範例、
> 常見錯誤）在 [`aidlc/spaces/default/knowledge/aidlc-quality-agent/test-case-authoring.md`](aidlc/spaces/default/knowledge/aidlc-quality-agent/test-case-authoring.md)。

---

## 0. 三十秒版本

1. 先判斷是**手動**還是**自動化**案例（見第 1 節）——一個行為只能有一個真實來源。
2. 照第 3 節或第 4 節的模板寫，**必要欄位一個都不能少**。
3. 跑 `python3 scripts/tcms_validate.py --all`，**ERROR 一律阻擋**。
4. 通過後才同步進 TCMS。

沒跑第 3 步就同步的案例，一律視為未完成。

---

## 1. 先分流：這個行為屬於哪一種

| | 手動案例 | 自動化案例 |
|---|---|---|
| 真實來源 | 本 repo 的 `manual-test-cases.md` → 同步進 TCMS | repo 的 spec code |
| 案例由誰建立 | `tcms_sync.py --file` | `kiwitcms-junit.xml-plugin`（從 CI 測試結果） |
| 描述來源 | 案例檔本身 | **code 旁的規格註解** |
| 何時用 | 無法或不該自動化 | 其餘一律 |

**無法或不該自動化**的四種情形，其餘都算「能自動化」：

1. 每跑一次都要花錢（呼叫 LLM 的路徑）
2. 依賴 CI 無法保證的外部服務（n8n webhook、需登入的 `claude` CLI）
3. 需要人的判斷（架構圖可不可讀、圖示對不對）
4. 需要真實環境狀態（`.env` 有殘值、瀏覽器已登入）

> ⚠️ **不要為自動化層已斷言的行為再寫一份手動案例。** 兩個真實來源＝兩份維護，
> 而其中一份必定悄悄過期。同理，**不要在 TCMS 手寫自動化案例的描述**——會被改
> 的是 code 那份，手抄的描述沒有任何機制會告訴你它過期了。

---

## 2. 必要欄位

以下六個欄位**每個案例都必須有**，`scripts/tcms_validate.py` 會逐項檢查，缺一即 ERROR：

<!-- required-sections: 目的, 受測介面, 前置條件, 測試步驟, 通過條件, 追溯 -->

| 欄位 | 必要 | 內容 | 檢查方式 |
|---|---|---|---|
| **目的** | ✅ | 這個案例保護什麼，一到兩句 | 非空 |
| **受測介面** | ✅ | 碰到的 API 端點、UI 路徑或 workflow | **比對 `openapi.json`、`App.tsx` 路由表與 `.github/` 下的實際檔案** |
| **前置條件** | ✅ | 步驟 1 之前需要的一切，可複製貼上 | 非空 |
| **測試步驟** | ✅ | 操作 ↔ 預期結果的表格 | 每列都要有操作與預期；預期不得是「正常」「成功」這類無法判定的詞 |
| **通過條件** | ✅ | 二元可判的判準 | 非空 |
| **追溯** | ✅ | 實作檔案、對應的自動化測試、PR、user story | **路徑與測試名稱回 repo 核對存在** |

### 選用欄位（依場景自行增刪）

| 欄位 | 什麼時候該有 |
|---|---|
| **背景** | 回歸案例**必須**有——寫出症狀、錯誤訊息逐字、以及既有自動化層為何沒抓到 |
| **測試資料** | 需要特定資料狀態時（例如要湊出第 2 頁的 21 個帳號） |
| **清理** | 案例會留下副作用時（建立的帳號、上傳的檔案） |
| **失敗徵兆與對應肇因** | 回歸案例強烈建議——它讓一次失敗的執行變成可行動的資訊，而不只是紅燈 |
| **備註** | 其他有助於執行或判讀的說明 |

場景不同可以增刪選用欄位，也可以自訂新欄位；**但必要欄位一個都不能少**。

---

## 3. 手動案例模板

檔案：`aidlc/spaces/<space>/intents/<record>/construction/tcms-test-cases/manual-test-cases.md`

直接複製下面的骨架填寫。**結構是機器解析的契約**，標題層級與 `- plan:` 位置不可改：

````markdown
## TC: <案例標題——它同時是 TCMS 的同步鍵，改標題等於建一個新案例>

- plan: <TCMS 測試計畫名稱>
- priority: P1

### 目的

<一到兩句：這個案例保護什麼。要指向一個真的會失敗的行為，
「驗證頁面正常顯示」這種對任何實作都成立的話不算目的。>

### 背景

<選用，但回歸案例必填。三件事缺一不可：
 1. 症狀——使用者看到什麼
 2. 錯誤訊息逐字
 3. 既有自動化層為何沒抓到（最重要，它同時說明了為什麼這是手動案例）>

### 受測介面

- API: `POST /api/auth/login` → 200 — 帳密驗證，成功時回 access_token
- UI: `/admin/users` — 使用者角色指派頁：表格、分頁導覽區
- Workflow: `.github/workflows/ci.yml` → pull_request — 四個 job 的執行與跳過
- 外部相依: <非本系統的依賴，如 n8n webhook；沒有就刪掉這行>

三種介面行別**至少要有一個**；`- 外部相依:` 不算，它描述的是依賴而不是受測面。

| 行別 | 比對對象 | 什麼時候用 |
|---|---|---|
| `- API:` | `openapi.json`（path、method 存在即硬錯誤；status 未宣告只警告） | 案例會打到後端端點 |
| `- UI:` | `frontend/src/App.tsx` 的路由表 | 案例會操作前端頁面 |
| `- Workflow:` | `.github/` 下的實際檔案；宣告的事件要真的在該檔的 `on:` 裡 | **受測對象是 GitHub Actions workflow 或 composite action**，既無端點也無前端路由 |

`- Workflow:` 是為了 intent `260822-gh-projects-sync` 新增的：它的交付物是 10 個
composite action 與 7 支 workflow，完全不在 web app 內，於是每一個手動案例都會撞上
「沒有列出任何 API 端點或 UI 路徑」。**正確的處置不是填一個假端點讓機械層過關**——
那正是這道檢查存在的理由所要防的事（見 `project.md` 的 `tcms-test-cases:c20`）。

### 前置條件

1. <可複製貼上的指令或設定>
2. <改過 `backend/.env` 的話，要寫明必須重啟後端——`--reload` 只監看 `.py`>

### 測試步驟

| # | 操作 | 預期結果 |
|---|---|---|
| 1 | <一個動作> | <一個可觀察的結果：畫面上出現什麼、log 裡哪一行、API 回什麼> |
| 2 | <一個動作> | <寫出「不得出現什麼」往往比「應該出現什麼」更有防護力> |

### 通過條件

- <二元可判：兩個人分別執行不會得到相反結論>

### 追溯

- 實作：`<檔案路徑>`
- 自動化對應：`<測試檔::類別::方法>`，或「無」
- PR／commit：<編號>
- User story：<id>
````

### 層級規則（踩過的坑）

案例內的小節一律 `###`；小節**之內**再分層用 `####`。曾經把「共用前置」與
「情境一／二」寫成 `###`，結果它們與「前置條件」「測試步驟」同級——解析器
看到的「測試步驟」是空的，而「前置條件」這個段落根本不存在。

---

## 4. 自動化案例：規格註解模板

自動化案例的描述**不在 TCMS 寫**，寫在 `test()` 前的註解裡，由工具渲染後同步：

```ts
/**
 * @purpose <這個測試保護什麼>
 * @given <前置狀態：seed 資料、需先建立的帳號、視窗尺寸>
 * @api POST /api/auth/login -> 200 | 帳密驗證，成功時回 access_token
 * @ui /admin/users | 使用者角色指派頁：表格、「最後活動時間」欄、分頁導覽區
 * @step <操作> | <預期結果>
 * @step <操作> | <預期結果>
 * @pass <通過條件>
 * @story <user story id>
 * @note <選用：這個斷言為何長這樣、它在防哪個具體的錯誤實作>
 */
test('<測試名稱>', async ({ page }) => {
```

| 標記 | 必要 | 說明 |
|---|---|---|
| `@purpose` | ✅ | |
| `@given` | ✅ | |
| `@api` / `@ui` | ✅ | 至少要有一個；兩者都會比對實際實作 |
| `@step` | ✅ | `操作 \| 預期結果`，一行一步 |
| `@pass` | ✅ | |
| `@story` | ✅ | |
| `@note` | 回歸案例 ✅ | 這個斷言為何長這樣、它在防哪個具體的錯誤實作 |

每個標記都可跨行（續行不加 `@`，會被接到前一個標記上）。

`@note` 渲染進 TCMS 時就是手動案例的「**背景**」段落——兩者是同一個角色，
所以回歸案例（標題或目的含「回歸」）沒寫 `@note` 會被驗證器擋下。

> **TCMS 案例名稱是 `<describe> › <test>`**，與 junit plugin 的
> `--summary-template '${name}'` 一致。改動 `describe` 或 `test` 的字串會讓
> 既有案例變成沒有執行結果的孤兒。

---

## 5. 驗證：寫完一定要跑

```bash
python3 scripts/tcms_validate.py --all
```

四類檢查，全部可判定：

| 類別 | 內容 |
|---|---|
| 必填欄位與格式 | 第 2 節的六個欄位都在；步驟表格每列都有操作與預期 |
| 空洞預期結果 | 不得是「正常」「成功」這類無法判定的詞；每案至少要有一個帶具體證據的預期 |
| 追溯目標存在 | 引用的檔案路徑與測試名稱回 repo 核對 |
| API/UI 比對實作 | 端點與 method 對 `openapi.json`，UI 路徑對 `App.tsx` 路由表 |

- **ERROR 一律阻擋**，退出碼 1。
- **WARN 要逐項判讀**，不得無視。最常見的是「OpenAPI 未宣告此狀態碼」，那通常
  代表端點缺 `responses=` 宣告，是真實的文件落差。
- 有一類 ERROR 不是案例缺陷：追溯指向的檔案在**另一支尚未合併的分支**上。那是
  真實的跨分支依賴，處置是說明依賴與確認合併順序，**不是把追溯改掉讓檢查過關**。

機械檢查過了**不代表案例是對的**。走 AI-DLC 流程的話，`/tcms-verify` 還有一層
語意審查（目的是否指向真會失敗的行為、受測介面是否漏列、規格是否與 AC 一致）。
沒走 AI-DLC 也請人工複核這幾點。

---

## 6. 同步進 TCMS

兩種來源分開跑，不可混用：

```bash
# 手動案例：建立＋更新
python3 scripts/tcms_sync.py --file <manual-test-cases.md> --dry-run
python3 scripts/tcms_sync.py --file <manual-test-cases.md>

# 自動化案例：只更新既有案例（案例由 junit plugin 建立）
python3 scripts/tcms_sync.py --spec frontend/tests/e2e/regression.spec.ts --dry-run
python3 scripts/tcms_sync.py --spec frontend/tests/e2e/regression.spec.ts
```

前置：`pip install tcms-api`，以及 `~/.tcms.conf`：

```bash
umask 077
read "U?TCMS 帳號: "
read -s "P?TCMS 密碼: "
echo
printf '[tcms]\nurl = https://tcms.danniel.cc/xml-rpc/\nusername = %s\npassword = %s\n' "$U" "$P" > ~/.tcms.conf
unset U P
```

（上面是 zsh 語法。bash 用 `read -p "TCMS 帳號: " U`。）

**一律先 `--dry-run` 看過再寫入。**

---

## 7. 五個最常見的錯誤

| 錯誤 | 為什麼是錯的 |
|---|---|
| 預期結果寫「正常」「成功」 | 執行的人無法判定，紅綠都說得通 |
| 只寫「應該出現 X」不寫「不得是 Y」 | 錯得很像對的結果會被放行——實測發生過（SNS 顯示成 Auto Scaling 圖示） |
| 把已自動化的行為再寫一份手動案例 | 兩個真實來源，其中一份會過期 |
| 回歸案例沒寫「既有測試為何沒抓到」 | 一年後沒人知道它在防什麼，會被當成冗餘刪掉 |
| 沒跑驗證就同步 | TCMS 上一份錯的案例會被當成已覆蓋的證據，錯誤的覆蓋感比沒有覆蓋更危險 |
