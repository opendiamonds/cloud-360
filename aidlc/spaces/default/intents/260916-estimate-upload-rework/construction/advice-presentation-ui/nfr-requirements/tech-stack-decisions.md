# Tech Stack Decisions — U9

| 決策 | 選擇 |
|---|---|
| UI | React 19＋Vite＋Tailwind（既有） |
| SSE | `fetch`＋`ReadableStream`＋逐行解析 `data:` JSON（對齊 Workspace／Assessment） |
| 測試 | Playwright ≥1（建議區狀態可辨） |
| 新依賴 | **無** |
