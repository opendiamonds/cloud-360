# Scope Definition — 問題檔

本檔為 `scope-definition` 階段的正式決策紀錄。所有 `[Answer]:` 由使用者填答，
AI 不得代答。回答格式為選項字母（可複選者以逗號分隔），或 `X` 並附文字說明。

## 已由上游定案、本站不重問

依 `scope-definition:260822-c5`，每一項都附可引用的定案原文：

- **產品邊界的能力集合** — intent-capture [Q9]=A：「確認：沿用這個工作計畫，
  產品邊界就是我描述的那些能力。」故本站**不重新開放邊界**，只在該集合內
  分 Must／Should／Could／Won't。
- **作業對象綁定三層階層** — [Q5]=B。本站不重問是否要建階層，只問它在第一版
  的優先序。
- ~~**成本能力只做交接介面** — [Q6]=C ＋ [Q11]=B。本站不重問要不要做成本計算。~~
  **（修訂 1 取代）**：上游已改為 **[Q12]=A**「編排既有的真實成本 agent，
  使用者第一版就問得到成本答案；不新建成本能力」。成本能力已存在
  （`backend/cost/`）。本站仍不重問「要不要做成本計算」（答案是不做、只編排），
  但**分級**因此重開，見 S11。
- **共享工作階段的涵蓋頁面** — [Q7]=A。本站不重問範圍。
- **決策者** — [Q8]=A：「單一決策者（你），無其他關係人」。故依
  `scope-definition:c5`，本站**不做 WSJF／RICE 數值評分**——沒有真實輸入的
  相對分數是虛假精確；以 MoSCoW ＋ 依賴序表達優先即足。
- **成本上限的承載方式** — feasibility [F13]=A（改在 OpenRouter 後台設）
  ＋ [F11]=B（無硬數字、原則盡量省）。本站不重問成本。
- **兩個技術試探** — [F6]=A,B（LangGraph 編排模型、記憶層資料模型）。
  本站不重問要試探什麼，但會問它們在交付序中的位置。

**不在此列、故本站要問的**：feasibility 的 [F8] 是單選題，使用者選了
「有預算或成本上限」，這**並未回答時程問題**——選項 B（有目標時程）未被
選中不等於「沒有時程」。依 `scope-definition:c1`，未選中的選項不得被當成
排除。故本站以 S7 補問硬期限。

## 待確認的能力集合（取自 intent-statement 的 Initial Scope Signal）

下列 10 項為 [Q9]=A 確認納入產品邊界的能力，本站的工作是替它們分級與排序，
不是增刪：

1. 統一入口的意圖識別與工作交辦
2. 跨功能共享的對話脈絡與自動切換
3. 可在子功能各自開啟新對話
4. 長短期記憶（語意記憶、程序記憶、情節記憶）
5. 多意圖識別
6. 多輪對話
7. 主動通知推播
8. 串流式互動（回覆逐步顯示）
9. 專案 → 系統 → 架構圖 階層
10. 成本／FinOps 交接介面（空殼，能正確回「尚未提供」）
    **（修訂 1 更名）**：現為「成本／FinOps 能力的編排」——辨識成本需求、
    以 HTTP 帶使用者 token 呼叫既有 `/api/cost/v1`、把 job 狀態事件轉譯為
    使用者可見訊息。[Q12]=A

---

## S1. 這四項哪些是第一版的 Must？（可複選）

Must 的定義：**缺了它，第一版就不算有東西**——不是「很重要」，是「沒有它
這個產品不成立」。`prioritization-frameworks.md` 的經驗法則是 Must 不應超過
計畫容量的 60%；若十項全是 Must，等於沒有排序。

- A. **統一入口的意圖識別與工作交辦**（能力 1）：使用者說一句話，系統判斷
  要做什麼、交給哪個功能 agent。
- B. **跨功能共享的對話脈絡與自動切換**（能力 2）：換頁後系統仍指向同一個
  作業對象，不需要重講。
- C. **專案 → 系統 → 架構圖 階層**（能力 9）：作業對象的識別基礎。含既有
  架構圖的遷移（風險見 RAID R-1）。
- D. **長短期記憶**（能力 4）：語意記憶、程序記憶、情節記憶三種。
- X. Other（請說明）

[Answer]: A, B, C, D  <!-- 2026-09-21T03:54:25Z | Mode: guided -->

## S2. 這四項哪些是第一版的 Must？（可複選）

- A. **串流式互動**（能力 8）：回覆逐步顯示。定案為改用 WebSocket [F4]。
- B. **多輪對話**（能力 6）：同一個作業對象上的連續往返。
- C. **多意圖識別**（能力 5）：一句話裡有多個意圖時能拆開處理。
- D. **主動通知推播**（能力 7）：系統主動告知，而非使用者輪詢。注意：其
  觸發情境與接收對象在 intent-capture 被記為未確認的假設。
- X. Other（請說明）

[Answer]: A, B, C, D  <!-- 2026-09-21T03:54:25Z | Mode: guided -->

## S3. 這兩項哪些是第一版的 Must？（可複選；也可以都不選）

- A. **可在子功能各自開啟新對話**（能力 3）：在 `/workspace` 或 `/assessment`
  頁面內開一段與入口頁無關的新對話。
- B. **成本／FinOps 交接介面**（能力 10）：空殼，但能正確把成本問題路由過去
  並回「尚未提供」[Q11]=B。
  **（修訂 1 註：本選項描述的空殼前提已不成立；本題作答 `A, B` 不改，
  能力 10 的最終分級由 S11 定案為 Must。）**
- X. Other（請說明）

[Answer]: A, B  <!-- 2026-09-21T03:54:25Z | Mode: guided -->

## S4. 沒被選為 Must 的那些，怎麼處置？

依 `scope-definition:c1`，未被選中不等於被排除，所以我不會自行推定。

- A. **統一放 Should**（重要但非必要，時間允許就做，不做也能上線）。
- B. **統一放 Could**（有更好，但明確次要）。
- C. **逐項再分**（我會針對剩下的項目再問一輪 Should／Could）。
- D. 尚未定義。
- X. Other（請說明）

[Answer]: A  <!-- 2026-09-21T03:54:25Z | Mode: guided | 前提為空：S1–S3 已把十項全選為 Must，故 Should 桶無項目，本答案目前無作用對象。由 S8 處置 -->

## S5. 有沒有要明確列入 Won't Have 的項目？

Won't Have 的意思是「本次明確不做，且這個決定要被記下來」——它是
`prioritization-frameworks.md` 說的「最有價值的分類」，因為它防止範圍蔓延。
依 `scope-definition:c1`，你若不想把某項列入 Won't Have，它會被記成
**未承諾**（不在範圍、不在排除清單、不推定未來去向），不會被我擅自補進去。

- A. **沒有要明確排除的**：十項都在範圍內（只是分級不同）。
- B. **有，我要點名**（請用 X 說明哪幾項，以及是永久不做還是本次不做）。
- C. **把 Should／Could 那些一律當成 Won't Have**：第一版只做 Must，其餘
  明確不做。
- D. 尚未定義。
- X. Other（請說明）

[Answer]: A  <!-- 2026-09-21T07:06:51Z | Mode: guided -->

## S6. 交付的排序偏好是什麼？

`workflow-planning-guide.md` 列出四種啟發式。已知的既有約束：兩個技術試探
（LangGraph 編排、記憶層資料模型）先行 [F6]；`team.md` 的 Q3 定案
`skeleton: off`，故不走 walking-skeleton 儀式。

- A. **風險優先**：不確定性最高的先做，讓後續決策建立在已校準的前提上。
  與已定案的兩個試探方向一致。
- B. **依賴序優先**：純依技術依賴排，能做的先做。
- C. **價值優先**：先做使用者最快感受得到的（例如意圖識別與交辦），
  基礎設施隨後補。
- D. 尚未定義——請你依前面的答案建議一個，我再確認。
- X. Other（請說明）

[Answer]: D  <!-- 2026-09-21T07:06:51Z | Mode: guided | 由 S10 承接：使用者要求本站依前面答案提出建議方案再確認 -->

## S7. 有沒有綁定特定能力的硬期限？

feasibility 的 [F8] 是單選題，你選了「有預算或成本上限」——那題並未回答
時程，而未選中的選項不得被當成排除，故本站補問。

- A. **沒有硬期限**：做到對為止，不綁日期。
- B. **有整體期限**（請用 X 說明日期，以及它是期望還是死線）。
- C. **有綁定特定能力的期限**（例如「某功能要在某日前可展示」；請用 X 說明
  是哪個能力、哪個日期）。
- D. 尚未定義。
- X. Other（請說明）

[Answer]: A  <!-- 2026-09-21T07:06:51Z | Mode: guided -->

## S8. 十項全為 Must——確認還是重新分級？

S1–S3 把 10 項能力全部選為 Must，S4 的「其餘統一放 Should」因此**無作用
對象**（Should 桶為空）。這不是文件瑕疵，是一個有後果的狀態：

- `prioritization-frameworks.md` 的 MoSCoW 規則逐字寫著「若everything is a
  Must Have，你沒有做排序——push back」，並建議 Must 不超過計畫容量的 60%。
- 實際後果：當交付階段遇到「時間或預算不夠，先出什麼」時，沒有任何已記錄
  的依據。那個決定仍然會被做出，只是由下游在沒有決策者輸入的情況下做掉。

**依 `scope-definition:c2`，有一項不視為矛盾也不降級**：主動通知推播被選為
Must，但其觸發情境與接收對象在 intent-capture 是未確認的假設。處置是把
「該參數於指定階段定案」升格為**上線前置依賴**，而非把該能力降級。

若要降級，本站提名下列三項為候選（依據而非偏好）：

- **多意圖識別**：單意圖處理已足以證明編排層可行；多意圖是同一機制的進階
  情境，不是另一種能力。
- **主動通知推播**：其參數未定（見上），且無任何已記錄證據顯示「被動輪詢
  不可用」。
- **子功能各自開啟新對話**：跨功能共享脈絡已是 Must；在子頁另開一段無關的
  新對話屬便利性，缺它不影響核心流程成立。

- A. **確認十項全為 Must**：S4 作廢（Should 桶為空）。我會在 intent-backlog
  明記「本次無排序空間」及其後果，供下游知情。
- B. **接受上述三項提名降為 Should**：其餘七項維持 Must。
- C. **維持十項全 Must，但另外指定應變序**：不改分級，但給出「若必須砍，
  先砍哪一個」的順序，讓下游在資源不足時有依據。
- D. 尚未定義。
- X. Other（請說明；例如你想降級的是別的項目）

[Answer]: X — 我要降別的項目（由 S9 承接點名）  <!-- 2026-09-21T04:00:47Z | Mode: guided -->

## S9. 你要把哪幾項降為 Should？（可複選）

S8 的回覆為「我要降別的項目」（Other），故本站不採用原提名，改由使用者
直接點名。下列為 [Q9]=A 確認的全部 10 項能力；**未被勾選者維持 Must**。

依 `scope-definition:c2`，若被勾選者含未定參數（例如主動通知推播的觸發情境），
降級與否與該參數無關——參數仍以「於指定階段定案」記為上線前置依賴。

- A. 統一入口的意圖識別與工作交辦（能力 1）
- B. 跨功能共享的對話脈絡與自動切換（能力 2）
- C. 可在子功能各自開啟新對話（能力 3）
- D. 長短期記憶（能力 4）
- E. 多意圖識別（能力 5）
- F. 多輪對話（能力 6）
- G. 主動通知推播（能力 7）
- H. 串流式互動（能力 8）
- I. 專案 → 系統 → 架構圖 階層（能力 9）
- J. 成本／FinOps 交接介面（能力 10）　*（修訂 1：此選項曾被選中而降為 Should；其後因前提失效，已由 S11 升回 Must）*
- X. Other（請說明）

[Answer]: G, J  <!-- 2026-09-21T07:08:19Z | Mode: guided -->

> **作答更正（2026-09-21T07:08:19Z）**：本題先前被記為 `A, B, C, D, E, F, H, I`（降 8 項、
> Must 僅剩 2 項）。本站當時依 stage-protocol §3 的矛盾偵測主動揭露該結果與
> [Q1]／[Q3]／[Q5] 的三處衝突，並明確提示「題目方向可能讀反」；使用者回覆
> 「沒反，就是這樣」，故照答案記錄。其後使用者主動更正為「應該是 8 個 Must、
> 2 個 Should」，確認方向確實讀反。本答案依此更正。
>
> **更正後的分級**：
> - **Must（8 項）**：能力 1 意圖識別與工作交辦、能力 2 跨功能共享對話脈絡、
>   能力 3 子功能開新對話、能力 4 長短期記憶、能力 5 多意圖識別、
>   能力 6 多輪對話、能力 8 串流式互動、能力 9 專案→系統→架構圖 階層。
> - **Should（2 項）**：能力 7 主動通知推播、能力 10 成本／FinOps 交接介面。
>   **（修訂 1 取代：能力 10 已由 [S11]=A 升為 Must，最終分級為
>   9 Must／1 Should，唯一的 Should 是能力 7。本行記載的是 S9 作答當下的
>   分級，保留不改。）**
>
> **先前記載的三處衝突（與 [Q1]／[Q3]／[Q5]）隨此更正一併失效**，不得帶入
> `scope-document.md`：Q1 的三個痛點、Q3 的三個成功指標量測對象、Q5 定案要建
> 的階層，其承載能力現皆為 Must，與上游一致。
>
> **仍然成立、與本更正無關的一項**：依 `scope-definition:c2`，能力 7
> 主動通知推播的觸發情境與接收對象在 intent-capture 是未確認的假設。它現為
> Should，該參數仍須「於指定階段定案」並記為上線前置依賴。
>
> **附帶事實（非衝突）**：8/10 為 Must（80%），高於
> `prioritization-frameworks.md` 建議的 60% 上限。本站已就「全為 Must」向
> 使用者 push back 一次（S8），使用者據以降了 2 項，此為其定案，不再重問。

## S10. 排序方案（本站提出，請確認）

S6=D：使用者要求本站依前面答案提出建議方案。

### 先行發現：能力間的技術依賴（更正後）

盤點能力間依賴時找到兩條**技術依賴**（依 `scope-definition:rev2-c8`，須明寫
是「技術依賴」還是「避免重工」——此二者皆為前者，**不可覆寫**）：

| 依賴 | 依據（逐字） |
|---|---|
| 成本／FinOps 交接介面（Should）→ 能力 1 意圖識別與工作交辦（Must） | [Q11]=B：「大腦能正確把成本問題**路由**到成本 agent，由它回覆一個明確的『尚未提供』」。需先辨識「這是成本問題」，空殼才有機會回覆 |

**修訂 1 註（依 `functional-design:c22`：依據被推翻但依賴關係不變）**：上表
第一列的依據已改為 [Q12]=A「大腦把成本類問題**路由**到既有 `/api/cost/v1`」，
且該能力已由 [S11]=A 升為 **Must**。依賴方向（能力 10 → 能力 1）與「不可
覆寫」的性質皆不變，故下方「Must 集合可獨立交付」的結論亦不變——能力 1
本就在 Must 內。原文不改，就地標註。
| 主動通知推播（Should）→ 能力 8 串流式互動（Must） | [F4]=B：「改用 WebSocket：**雙向，可承載主動通知推播**」。推播的通道即該 WebSocket |

**更正註記（2026-09-21T07:08:19Z）**：本節先前寫的是「兩項 Must 各自依賴一項 Should，
故 Must 集合無法獨立交付」。該結論建立在 S9 更正前的分級上，隨分級更正一併
失效。更正後兩條依賴的方向皆為 **Should → Must**，即被依賴者都在 Must 內，
**Must 集合可獨立交付**，沒有阻塞。兩條依賴事實本身不變，變的是它們的後果。

**未主張者**：推播是否亦依賴能力 1，取決於「什麼事件觸發推播」——該參數在
intent-capture 被記為未確認的假設，故本站不替它假設任何依賴。

### 建議方案

**風險優先，外加不可覆寫的技術依賴序。**

理由逐條對照已定案事實：

- **無硬期限**（S7=A）→ 時間不是約束，故不採價值優先（價值優先的意義在於
  時間受限時先交付可感知的價值）。
- **兩個技術試探已定案先行**（[F6]=A,B：LangGraph 編排模型、記憶層資料模型）
  → 與風險優先同向。
- **RAID R-1（既有資料遷移）影響最大且無試探涵蓋**（[F10]=A 指派給設計階段）
  → 風險優先要求它在依賴它的工作之前被處理。
- **`team.md` Q3 定案 `skeleton: off`** → 不走 walking-skeleton 儀式，第一個
  交付單元照常規走。
- **不做 WSJF／RICE 數值評分**（`scope-definition:c5`，單一決策者）→ 排序以
  啟發式＋依賴序表達，不產生分數。

- A. **採用「風險優先 ＋ 技術依賴序疊加」**（本站建議）。
- B. **只要技術依賴序**：不疊加風險優先的額外排序，能做的就做。
- C. **我重新選一個啟發式**（回到 S6 的風險優先／依賴序優先／價值優先）。
- D. **先別定，留到 delivery-planning**：本站只記錄兩條技術依賴，排序交給
  下游。
- X. Other（請說明）

[Answer]: A  <!-- 2026-09-21T07:09:49Z | Mode: guided -->

## 修訂 1 的前提更新（2026-09-21）

上游 intent-capture 與 feasibility 皆已以 Modify 模式修訂。與本站相關的變動：

- **[Q12]=A**（取代 [Q6]=C／[Q11]=B）：大腦編排**既有的真實成本 agent**，
  使用者第一版就問得到成本答案；不新建成本能力。成本能力已存在
  （`backend/cost/`，`/api/cost/v1` 下 10 條端點）。
- **[Q14]=A**：成本答案就地在入口頁呈現，`/cost` 頁不加入共享工作階段範圍。
- **[Q13]=B**：大腦自建獨立的編排執行層。
- **[F14]=A**：大腦以 HTTP 帶使用者 token 呼叫成本能力（feasibility 約束
  C-S5）。**[F15]=A**：成本 job 的狀態事件轉譯進大腦訊息流（C-S6）。
- 上游 `stakeholder-map.md` 已把成本／FinOps 關注者由**間接**改為**直接**
  服務對象。

## S11. 能力 10 的 MoSCoW 分級要不要改？

**本題由上述前提變動逼出。** [S9] 把能力 10 降為 Should 時，它的內容是
「成本／FinOps 交接介面（空殼）」——一個只會回「尚未提供」的殼。該前提已
不存在。而上游同時把成本關注者改為**直接**服務對象、明言「第一版就問得到
答案」。兩者並存時，一個被寫成直接服務對象的角色在第一版可能完全沒被服務到
——這與 [Q10]／[Q11] 當初處理的張力**形狀相同**，故依 `project.md` 的
`intent-capture` 同型張力規則加開本題由使用者定案，不由 conductor 自行裁量。

- A. **升為 Must（9 Must／1 Should）**：與「直接服務對象、第一版就問得到
  答案」一致。原本降為 Should 的依據（只是個空殼）已不存在，且它依賴的成本
  agent 本來就已建好，實作量只剩「辨識 ＋ 路由 ＋ 狀態轉譯」。
- B. **維持 Should**：保持 8／2。代價為與剛核可的上游直接矛盾，需回跳修改
  「直接服務」措辭。
- C. **升 Must 但降一項現有 Must 當出（維持 8／2）**。
- D. 尚未定義。
- X. Other（請說明）

[Answer]: A  <!-- 2026-09-21T10:31:44Z | Mode: guided | 修訂 1 -->

採 A 的已揭露代價（提問時列於選項說明）：Must 佔比由 8／10（80%）升為
9／10（90%），更高於 `prioritization-frameworks.md` 建議的 60% 上限。本站
已於 [S8] 就此向決策者 push back 過一次並獲其定案，本輪為前提變動後的再次
定案，如實記載佔比事實，不再質疑。

## Consolidated Summary Confirmation

Does this all look correct before I generate the artifact?

- Looks correct
- Request changes

<!-- 修訂 1（2026-09-21T10:31:44Z）於本確認之後新增 S11，依 project.md
     `requirements-analysis:260822-ra-L3` 清空並重新取得確認。 -->
[Answer]: Looks correct
