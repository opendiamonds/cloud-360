# Intent Backlog — 統一入口大腦

本文件把 `scope-document.md` 的 10 項能力轉成有序的 proto-Unit 清單，供
下游 units-generation 與 delivery-planning 承接。

**排序依據**：[S10]=A 風險優先 ＋ 不可覆寫的技術依賴序。
**不做數值評分**：依 `scope-definition:c5`（單一決策者、依賴序已定），
以 MoSCoW ＋ 依賴序表達優先，不產生 WSJF／RICE 分數。

proto-Unit 是**能力層級的工作塊**，不是最終的工作單元切分——切分是
units-generation 的工作，其判準是「驗證方式與失敗模式是否同類」
（`units-generation:c6`），與本站的分級不同軸。

## 先行項（非 proto-Unit）

兩項技術試探已於 feasibility 定案（[F6]=A,B），排在所有 proto-Unit 之前。
它們是用完即丟的探測，不進正式程式碼、不求品質。

| 先行項 | 要回答的問題 | 來源 |
|---|---|---|
| P-1 LangGraph 編排試探 | LangGraph 能否乾淨包住既有那些以 SSE 回應的 agent 端點 | [F6] |
| P-2 記憶層資料模型試探 | 三種記憶各自的形狀與查詢方式；換映像取得向量檢索的代價 | [F6]、feasibility [S9] |

## proto-Unit 清單（依交付序）

| 序 | proto-Unit | 能力 | 分級 | 排序理由 | 阻擋於 |
|---|---|---|---|---|---|
| 1 | **專案 → 系統 → 架構圖 階層** | 9 | Must | **風險優先**：承載 RAID R-1（既有架構圖遷入新階層），是本 intent 影響最大且**無試探涵蓋**的風險；且 `schema_rbac.sql` 只在空 volume 執行，既有環境需手動遷移。愈晚做，建立在其上的東西愈多 | — |
| 2 | **統一入口的意圖識別與工作交辦** | 1 | Must | 產品核心；被能力 10 以技術依賴指向。P-1 試探的直接對象 | P-1 |
| 3 | **串流式互動** | 8 | Must | 被能力 7 以技術依賴指向；亦是「首字回應時間」指標的量測基礎。改用 WebSocket（[F4]=B） | — |
| 4 | **長短期記憶** | 4 | Must | P-2 試探的直接對象；同時承載管理者「誰改了什麼」的需求（[Q10]=C） | P-2、1 |
| 5 | **跨功能共享的對話脈絡與自動切換** | 2 | Must | 需要作業對象（序 1）與記憶（序 4）都在位才有脈絡可共享 | 1、4 |
| 6 | **多輪對話** | 6 | Must | 建立在共享脈絡之上的連續往返 | 5 |
| 7 | **多意圖識別** | 5 | Must | 意圖識別（序 2）的進階情境，同一機制的擴充 | 2 |
| 8 | **可在子功能各自開啟新對話** | 3 | Must | 需要共享脈絡機制存在，才談得上「另開一段不共享的」 | 5 |
| 9 | **成本／FinOps 交接介面（空殼）** | 10 | Should | **技術依賴**：需要意圖識別先能辨識「這是成本問題」（[Q11]=B 的「路由」） | 2 |
| 10 | **主動通知推播** | 7 | Should | **技術依賴**：推播通道即 WebSocket（[F4]=B）。另有未定參數（觸發情境與接收對象），列為上線前置依賴 | 3 |

### 排序中的判斷說明

- **序 1 置頂是風險優先的直接後果**，不是依賴造成的。階層本身不依賴任何
  其他 proto-Unit；把它排第一，是因為它承載的遷移風險沒有任何試探涵蓋
  （[F10]=A 指派給設計階段），而風險優先要求最高不確定性先被處理。
- **序 3（串流）不被任何項目阻擋**，可與序 1、2 並行。它排在序 4 之前，
  是因為序 10（推播，Should）以技術依賴指向它。
- **兩項 Should 排在最後**，但**不是因為它們是 Should**——是因為它們各自
  被技術依賴綁在序 2 與序 3 之後。即使升為 Must，順序也不會改變。
- **序 5 之後的四項（6、7、8 與兩項 Should）彼此無依賴**，順序由風險優先
  與「避免重工」決定，下游可在記明緩解方式的前提下調整這四項的相對次序；
  序 1–5 的依賴邊與序 9、10 的依賴邊為**技術依賴，不可覆寫**。

## 未承諾項

依 `scope-definition:c1`，[S5]=A 確認**沒有任何項目被列入 Won't Have**。
本清單的 10 項全數在範圍內，無未承諾項，亦無排除項。

## 上線前置依賴（承自 scope-document）

| 前置依賴 | 綁定 proto-Unit | 定案階段 |
|---|---|---|
| 主動通知推播的觸發情境與接收對象 | 序 10 | 待指定 |
| 三個成功指標的門檻值 | 序 2、3、5 的驗收 | requirements-analysis（[F7]=B） |
| episodic memory 的保存期限值 | 序 4 | 待指定 |
| 語意記憶是否採向量檢索 | 序 4 | 設計階段 |

## 對下游的指派（承自 feasibility，本站重申）

以下事項在 feasibility 已指派，因其綁定的 proto-Unit 在本清單中有明確序位，
本站重申以免無聲落空：

| 指派對象 | 事項 | 綁定序位 |
|---|---|---|
| application-design 或 functional-design | 既有架構圖遷入新階層的具體步驟與回復方式 | 序 1 |
| requirements-analysis | 三個成功指標的門檻值定案 | 序 2、3、5 |
| 設計階段 | 外部成本上限觸發時的系統行為 | 全域 |
| 設計階段 | 兩種串流機制（新 WebSocket 與既有 3 個 SSE 端點）並存的邊界 | 序 3 |

## Assumptions & Open Questions

- 本清單的序位是**能力層級**的交付序，不是工作單元的切分結果。
  units-generation 的切分判準（驗證方式與失敗模式是否同類）與本站的分級
  不同軸，切分後的單元數與本清單的 10 項不必一致。[assumption]
- 序 5 之後四項的相對次序未經依賴固定，下游可調整；本站以風險優先給出一個
  基準序，非唯一解。[assumption]
- 序 1 的遷移風險沒有試探涵蓋，其可行性在實作前無執行期證據。[assumption]
