# Code Generation Questions — U-11 README 指路段落

<!-- Stage: code-generation（Construction，per-unit）· Unit: U-11-readme-pointer -->

## Plan Approval

4 步計畫，交付 `README.md` 的**純插入**段落（`## Requirements Source`，置於 `## Documentation` 之前），含 Project #16 連結並說明該看板是需求清單的正本。

**一項需一併裁決的判斷**：本單元**不新增測試檔**。兩條規則的驗證都是一次性交付檢查（文字比對、`git diff --numstat` 讀數），本單元沒有可執行的行為需要回歸保護；R-1 的關鍵字保護已由 `validate_repo_contract.py` 的 `REQUIRED_TEXT` 常態承接。若你認為該把 `projects/16` 加進 `REQUIRED_TEXT` 以取得常態保護，選 Request Changes。

**選項**：

- **Approve Plan** — 依計畫產生變更
- **Request Changes** — 修改計畫後重新送審

[Answer]: Approve Plan  <!-- 2026-08-30T07:33:40Z（讀自 date -u）· 人工核可，含「不新增測試檔」的判斷 -->

