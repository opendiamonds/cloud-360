# Stage Diary — code-generation

<!-- Stage: code-generation（Construction，per-unit）· 由 orchestrator 維護，不手改 -->

## Interpretations

- U-1 的驗證方式限「純文字 fixture 斷言，不得有網路或檔案系統 I/O」。把全部邏輯內嵌進 `action.yml` 的 `run:` 會讓 fixture 斷言必須先起 workflow（或裝 `act`），正是該約束要避開的東西。因此讀成：**邏輯需與 YAML 介面分離**，`map.sh` 只從環境變數取輸入，讀 fixture 檔的是測試框架而非受測邏輯——如此「零 I/O」是可測的事實而非宣稱。此決定擴充了 `unit-of-work.md` 字面的交付清單，故送 Plan Approval 由人工裁決，未自行認定（2026-08-30T06:21:17Z 核可）。
- `unit-of-work.md` 的 U-10a 完成判準寫的是「`paths-ignore` **或等價手段**」。機制由 `paths-ignore` 換成 concurrency 分組 ＋ job 層 `if:` 落在「等價手段」內，不構成對已核可上游的改寫。

- 2026-09-03T16:20:18Z — U-3 的四項介面判斷上游未逐字指定，落法寫進 plan 並送 Plan Approval 裁決，不自行認定：①`write_status` 的 `expected: ItemState` 只比對 `status` 欄位（mutation 只觸及 Status，全欄位比對會讓無關並行變更永久 Abort 合法寫入）；②R-3.1 的 record 檢查以 `existing_binding` input 承接（record 知識屬 U-4，不外洩進 U-3）；③R-6.2 的標記引用以執行期自 `block.sh` 萃取賦值行實現（bash 跨 action 無 import，萃取即引用、單一真實來源仍在 U-2），輔以一致性測試鎖住；④`create_item` 的 issue 標題為選填 input 預設 `intent_id`（標題慣例的正式決定留給擁有呼叫端的 U-6）。
- 2026-09-03T16:20:18Z — U-3 測試分兩層：stub（PATH shim 偽裝 `gh`，離線，覆蓋 live 構造不出的分支——R-1.4 多筆、四列錯誤分類、`NOT_FOUND` 防誤映射）＋ live（對測試看板 #23，逐條完成判準）。依 ADR-0016 §6：R-1.4 以 stub 誠實構造反例，不發明假的 live 觸發途徑。
- 2026-09-04T17:22:35Z — U-4 計畫前的唯讀查證推翻了完成判準的一半前提：`ut` 的 branch protection 為 `enforce_admins: false`、無 required checks、無 push restrictions，而憑證帳號 `permissions.admin: true`——擁有者 token 直推 `ut` **不會被平台擋下**（`main` 因 `enforce_admins: true` 會）。「推 `ut` 被分支保護拒絕回 `Rejected`」對本憑證無法由平台滿足，故計畫把 R-3.1 落成介面層防線（`branch` 為 `ut`／`main` 即 `rejected`／`policy`，任何 git 動作之前），並把 live 測試的進場防呆定為「拒絕任何以 `ut`／`main` 為目標的步驟」——與 U-3 的 SEC-3 同型：隔離靠名稱，不靠權限。是否對 `ut` 開啟 `enforce_admins` 屬 repo 設定層決定，標給 Bolt 1 gate，不在本單元裁定。
- 2026-09-05T00:41:59Z — U-4 的 `attempts` 語意：R-3.5 原文「重試 3 次後仍非快轉」可讀成 1＋3＝4 次 push，已核可計畫 Step 6 寫的是「`attempts=3`、hook log 計 3 次」；以計畫為準（`MAX_RETRIES=3` ＝ 3 次 push 用罄），歧義寫進 code-summary 未完成項目第 4 項留給 gate。
- 2026-09-05T01:55:16Z — U-5 的 `resolve_if_open` 落成**批次鍵**介面：U-6 R-6.1a 要求「對每個待關閉鍵各呼叫一次」、U-5 R-3／[Q2=A] 要求「每輪一次查詢」，逐鍵呼叫而每次都列舉正是 Q2=B 被否決的 30 次呼叫。批次讓兩條規則同時成立且每個鍵的語意與單獨呼叫相同（不存在即 no-op），簽章語意未改。經 Plan Approval 裁決 1 核可。
- 2026-09-05T03:15:55Z — U-5 的 `truncate_bytes` 在 iteration 2 被抓到對**畸形**輸入仍留下不完整序列，修法是把「掃描一次算出剝除量再剝一次」換成「逐位元組收斂」。寫新測試時發現原註解「從不產生無效 UTF-8」對畸形輸入本來就不可能成立（不需截斷時原樣回傳、畸形位元組若在前綴中間無法處理），故一併把承諾修正為精確的責任邊界：**不因截斷而產生不完整序列**。處置順序很重要——先修真缺陷（尾端），再把承諾對齊，而不是把測試放寬到看不見缺陷。
- 2026-09-05T03:41:53Z — U-5 `truncate_bytes` 的第三個 Critical（分類表只驗長度、放行 overlong／surrogate／超界）修法是換成完整的 UTF-8 合法性表。**同輪一併修的方法論缺陷比缺陷本身更要緊**：測試的 oracle 與受測邏輯共用同一張表，兩邊對這三類序列一起誤判，測試結構上無法揭穿自己——這正是前兩輪連續漏掉它的原因。oracle 已改用 Python 自己的解碼器，與受測邏輯完全脫鉤。教訓：**oracle 只要與受測對象共用任何判定邏輯，該邏輯的錯誤就是這組測試的盲區**，且盲區的大小等於共用的那部分。
- 2026-09-05T04:09:56Z — U-5 收斂於 iteration 4（READY）。四輪各出一個 Critical 全落在 `truncate_bytes`，來源分別是實作 bug、修正缺口、既存漏審、以及**測試覆蓋缺口**（規則寫對了但沒有東西守著它不被改壞）。最後一項只有突變測試發現得了——「測試全綠」在那三條條件式規則上先前沒有資訊量。這條線索的完整形狀：oracle 與受測邏輯共用判定 → 盲區；規則沒有對稱突變 → 盲區。兩者都不是「測試不夠多」，是「測試問錯問題」。
- 2026-09-05T04:36:23Z — U-10a 開工前查證推翻已核可設計的一半：nfr-requirements 的 Q 選項 A 以「同步以 bot 身分推送，`github.actor` 與開發者不同」為唯一依據，而 ADR-0016 §1 已把身分定為**擁有者帳號 token**，實測開發者推 code 與同步機制的 `github.actor` **同為 `opendiamonds`**。commit 的 author 可設為 `aidlc-sync`（U-4 的 SEC-4），但 `github.actor` 由推送用的憑證決定、與 commit author 無關——這兩件事在上游被混為一談。這是 `functional-design:c10`（新增「偵測 X 狀態」的規則前先推導 X 是否可達）的又一次實證，且該缺口是**兩份已核可產出各自都對、合起來才錯**：nfr-requirements 寫選項時 ADR-0016 還沒定案。
- 2026-09-05T05:01:33Z — U-10a 的 guard 在計畫外多了第七項檢查（`MARKER-1`），觸發者是突變驗證本身而非審查：M6（改 `record.sh` 的 `SYNC_MARKER`）在原版 guard 下**是綠的**。也就是 U-4 若改標記，`ci.yml` 硬編碼的 `[aidlc-sync]` 就悄悄失配、每顆同步回寫都判 `is_sync=false`、四道關卡照跑——本單元等於沒做且不紅燈。修法沿用 `SEC-1b` 已建立的「從 `record.sh` 推導而非自抄一份」形狀，是同一模式的第二次套用，非新設計。教訓的可執行形式：**跨單元共用的每一個字面常數，都要問「哪一邊改了，另一邊會不會無聲失配」**——路徑那一半（`SEC-1b`）當初想到了，標記那一半沒有，兩者是同一個問題的兩面。
- 2026-09-05T06:22Z — U-10a 的核心教訓，值得單獨記：**iteration 1 加 `MARKER-1`（文字斷言）有洞；iteration 1 修它（仍是文字斷言）仍有洞；iteration 2 才看出問題不在斷言寫得不夠細，而在種類選錯了。** 拿文字／結構斷言去抓行為缺陷，補得再多都會有下一個變形——reviewer 只用「把 `if` 的兩個分支對調」就繞過了全部 19 項檢查（`grep` 呼叫一個字元沒動），而後果比標記漂移嚴重得多：四道關卡對**所有**正常 commit 永久失效。換成行為測試（抽出腳本實際跑、斷言判定值）後，四種繞過全部立刻紅。**可執行判準：當一條規則要守的是「這段程式做出什麼判定」，斷言就必須執行它；只有規則要守的是「這個檔案長什麼形狀」時，文字斷言才是對的工具。** 同型失誤連續三次之後才換種類，這個延遲本身是主要成本。
- 2026-09-05T14:52:48Z — U-6 的核心教訓，與 U-10a 的那條互補：**一個欄位同時扛兩種語意時，任何一條「刻意不寫」的分支都會讓它裂開，而在該欄上調整條件永遠只能修好一邊。** `last_status` 先後被改過兩次、方向相反——無條件寫（誠實性壞：宣稱沒發生的寫入 ⇒ 假 `Aborted`）、條件式寫（收斂性壞：比對永遠不回零 ⇒ 每次 push 產生真實 commit）。**兩次都在同一欄上調條件，所以必然只能顧一邊。** 正解是拆成兩欄（`last_status` 記判定、`last_written_status` 記寫入），而這個選項在 (C) 的原始分析裡**本來就列著**——上一輪選了成本較低的那個，繞了一圈才回來。可執行判準：規則若自己說「這兩種語意不可互相取代」（R-5.8 逐字），那它們就**不能共用一個欄位**，這不是實作偏好而是規則的直接後果。
- 2026-09-05T17:10:13Z — U-7 的 Critical 是一個**流程**缺陷而非實作缺陷，形狀值得單獨記：`open-items.md` 裡有一項 **C-7.1**（Critical，期限「Bolt 1 開工前」，修法早已寫定、逐字指名落點在 R-6.1／R-6.2），而我在開工前查證**完全沒讀那個檔**，於是把它當成新發現重新描述，並**降格為「潛伏，Bolt 2 gate」**——等同把一個已過期的強制關卡改寫成更弱、更晚的關卡。補做全項核對後發現第二層代價：**M-7.1／M-7.2 的落點逐字寫著 `code-generation`（指派給這個 stage）**，U-6 在不知情下做對了它們，卻沒有任何產出記錄「已關閉」，下一個人讀 `open-items.md` 會以為還開著。**可執行判準：任何 per-unit stage 的開工前查證，必讀 `construction/functional-design/open-items.md` 全表，並在 summary 逐項報告「已涵蓋／未涵蓋／不適用」——不是只查被點名的那一項。** 四個姊妹單元（U-1／U-2／U-10a／U-11）的 summary 都引用了它，U-6 與 U-7 沒有，而這個差異本應在檔案集合一致性自檢（第 4 項）被看出來。
- 2026-09-06 — U-7 iteration 2 補上 C-7.1 教訓的第二層：我自稱做了「`open-items.md` 全項交叉核對」，但只核對到**條目層級**（四項各自是否被涵蓋），**沒有追條目內部的但書**——`open-items.md:137` 自己寫著「該欄現有三個寫者（R-5.4／R-6.1／R-6.8），**本輪只對兩個做了語意對齊**」，那句話直接點出第三個寫者未對齊，而我修完 R-6.1 就收手了。**可執行判準：核對 open item 時，條目的每一個但書、每一個「只對 N 個做了」的限定語，都是一個獨立的待查項。**
- 2026-09-06 — 跨位置傳播失敗又一次，這次落在**單一檔案內部**：C-7.1 的修正在 `impl.yml` 留下三處未同步的殘跡，其中最嚴重的是**檔頭「交還 gate 的三項」摘要清單**仍說缺陷還在。reviewer 判 Major 的理由值得記：**摘要區塊的明文用途就是讓人不必讀本體**，所以它與本體矛盾比兩份文件矛盾更危險。附帶：reviewer 找到兩處，我按事實（而非它給的行號）重掃時找到**第三處**——這佐證了「按事實掃、不按被點名的位置掃」是對的。

## Deviations

- code-generation 開跑前先處理了兩項 nfr-requirements 登錄的 Critical（N:C-1 U-10a 的 `paths-ignore`、N:C-3 U-9 的檔名樣式），而非依登錄順序帶到 Bolt gate。理由：兩者都是**機制在自己指認的失敗路徑上無效**，照設計產生程式碼等於把已知失效的機制寫成程式碼，且兩者失效時都不紅燈。人工裁決選擇先解（2026-08-30T06:21:17Z 之前）。

- code-generation 為 per-unit，本 session 完成 **U-1、U-11、U-2** 三個單元（皆 reviewer READY），於 **U-3** 停下。此為人工裁決的暫停點，非引擎路由——引擎仍會在下次 resume 時給出 U-3。
- 三個單元的 reviewer 共判 4 個 Critical／Major 等級發現，其中 **3 個形狀相同**：規則寫在文件上、看起來已經在守，實際上守不到它自己宣稱的東西（U-9 的檔名樣式、U-10a 的 `paths-ignore`、U-2 的互鎖漏掉序列化面）。第四個（R-4.2 宣稱擋「更新快照沒 bump」而實際恆綠）是我在修第三個時實測撞出來的。

- 人工裁決要求在核可 ADR-0016 之前補測其 Alternatives B（fine-grained PAT），理由是該選項的否決理由逐字為「未經實測」而它是唯一能改善 IAM 面的低成本候選。**補測確認 B 不可行，但過程推翻了 ADR-0016 自己 §7 的「收斂手段已耗盡」**——為了取得第一手佐證而做的 scope 探查，揭露 `public_repo` 可取代 `repo`。ADR-0016 已整節改寫並新增 PRE-1-c（2026-08-30T23:31:09Z）。

- 2026-09-04T00:51:05Z — U-3 派工的規則 bundle 以 harness 既有機制傳遞而非逐字貼進 brief：Claude Code 對每個 dispatched agent 自動注入專案 CLAUDE.md 的 import 鏈（`@.claude/rules/aidlc.md` → org/team/project/phases 全文），內容與 load-steering bundle 逐檔相同；brief 中明文指示以該規則集為準。與 stage-protocol §5「paste verbatim」字面不同、資訊等價，記為 deviation。
- 2026-09-04T17:29:14Z — U-3 reviewer iteration 1 的 Critical（`write_status` 對 item 不在板上回契約外的 `Failed`）由 orchestrator 定案改為 `Aborted`，**未另開 Plan Approval**：這個修法不新增任何契約值、只收回一個契約外的值，且 U-6 已核可的 R-5.12 直接涵蓋其處置；與本單元四項介面判斷（上游未指定、需人選一邊）性質不同。已在 code-summary 的「未完成項目」第 6 項揭露它是對上游 `Aborted` 定義的詮釋，留給 gate 判斷是否要在契約上明寫。
- 2026-09-04T17:29:14Z — 上一 session 在 reviewer iteration 1 回 NOT-READY 後結束，`REVIEW_COMPLETED` receipt 未記；本 session 於 2026-09-04T17:11Z 補記（`--iteration 1 --verdict NOT-READY`），時間戳為補記時刻而非審查時刻（審查時刻 2026-09-04T01:53:47Z 在 code-summary 的 Review 段）。
- 2026-09-05T00:41:59Z — U-4 的三方鍵層合併在**每一次**嘗試都做，不只非快轉重試時（計畫 Step 5.3 字面為「複製檔案進 worktree」）：origin 在本輪開始前已領先時，複製會靜默抹掉並行寫入者的欄位且不觸發非快轉——是 R-3.5 要防的資料抹除換了入口。lead 定案、orchestrator 核對後接受，記為對計畫的一項偏離並以 `test_cap_origin_ahead_first_attempt_merges` 鎖住。
- 2026-09-05T01:09:11Z — U-4 reviewer 判 READY 後仍當輪修掉兩項 Minor（無引號展開的 glob、原始 push stderr 進公開 workflow log）：READY 不強制再修，但兩者成本低、且後者落在 public repo 的洩漏面。修第二項時實測撞出一個 reviewer 與既有測試都沒抓到的缺陷——`scrub_git_stderr` 對 git 的 client-side 拒絕行（行首有一個空格的 `! [rejected]`）完全不命中，使非快轉耗盡時的 `message` 沒有任何原因。一併修並以突變驗證。
- 2026-09-05T01:55:16Z — U-5 的 `closed_numbers` 由計畫寫的「換行分隔」改為空白分隔：`emit` 的 stdout `name=value` 單行通道與 `$GITHUB_OUTPUT` 的 heredoc 通道對多行值解讀不一致，而呼叫端與 harness 都讀 stdout。三處逐字寫明並以 `test_stdout_and_github_output_agree` 鎖住。另新增計畫 Step 1 漏列的 `result` output（Step 4 要求寫它但 outputs 清單沒有）。
- 2026-09-05T02:32:22Z — U-5 reviewer iteration 1 的 Critical（`truncate_bytes` 的 UTF-8 邊界回退）由 orchestrator 先自行複驗才派修，複驗發現**範圍比 reviewer 報告的更廣**：不只 4-byte 字元產出無效 UTF-8，任何截斷點落在字元邊界上都會誤刪一個完整字元（3-byte 與 2-byte 各實測一例）。派工單直接把正確演算法（比對 `k` 與 `need`）寫進去，lead 複驗時又補出第三個邊界案。教訓：reviewer 給的是**一個**反例，修之前要先問「這個反例的類是什麼」——照單修一個反例會漏掉同類的其餘。
- 2026-09-05T02:52:13Z — U-5 reviewer iteration 2 的第一次派工因 API 額度限制中斷（session limit，subagent 模型 claude-sonnet-5），**未寫入任何內容**：`code-summary.md` 只有 iteration 1 的 Review 段、工作樹完好、stub 仍 33/372/0。audit 因此會出現同一 iteration 的兩筆 `REVIEW_REQUESTED`——如實保留，它記錄的是派工確實發生過兩次。
- 2026-09-05T03:15:55Z — U-5 的 reviewer 輪次上限（2）在 iteration 2 已用罄，但依 `project.md` 的 `application-design:c4` 不以「用罄即 proceed」放行：本輪 Critical 正是 iteration 1 修正動作留下的缺口（新演算法自己的回看窗上限），且落在 SEC-2 的公開 issue 路徑。修完另跑一輪**驗證輪**，該輪不計入原始上限。
- 2026-09-05T03:41:53Z — U-5 的驗證輪停止判準依 `project.md` 的 `functional-design:c18` 在下一輪**開始前**訂定並寫進 code-summary：下一輪若找到的 Critical 是本輪修正引入的就修再驗；若又是既存漏審（同一函式的第四個獨立缺陷）就**停止迴圈**、寫成 open item 帶進 Bolt 1 gate，屆時要決定的是這個函式該不該換實作策略而非再修一次；只找到 Major 以下就收斂。三輪的缺陷來源分別是實作 bug、修正缺口、既存漏審——修正動作沒有在製造新缺陷，是審查在挖深，故續跑而非停止。
- 2026-09-05T04:36:23Z — U-10a 的 [US:S-1 AC 7] 第一半（既有 run 不被取消）在 `pull_request` 事件下**結構性無解**：要分辨這次 synchronize 是同步機器或人造成的，所需資訊（commit 訊息、不同身分）都不在事件酬載裡，而 concurrency 在任何 job 執行前就決定。人工裁決選「接受 PR 側會被取消」（2026-09-05）：push 側改用 `paths-ignore`（比原方案更強，run 根本不建立）、PR 側加前置 gate job 讓四個 job 全 skip。**AC 7 需回頭改寫**，指派與確認人寫進 code-summary。被否決的三案各自的代價亦記於問題檔。
- 2026-09-05T05:01:33Z — U-10a 的計畫 Step 5 寫「機械斷言六件事」，實際落地七件；Step 6 原訂「至少三條」突變，實際跑 8 條。Step 5／6 本文不回改（已核可計畫原文），以計畫檔的 Revision 段與 `code-summary.md` 的「對計畫的偏離」段承接。
- 2026-09-05T05:05Z — U-10a 的送審前自檢第 5 項（跨檔傳播）第一次寫下時宣稱 `grep -rn "六件事\|六項"` 零殘留，複跑實測**命中 1 處**（`check-ci-yml.py:21`，刻意的歷史引用）。已就地更正為實測值並註明判定理由。這是「可算／可查的事實先跑再寫」在自檢報告本身的一次失守——自檢項的結論同樣是一個待驗證的主張，不因為它出現在自檢表裡就免驗。
- 2026-09-05T06:22Z — U-10a 停止於 reviewer iteration 2，**未依 iteration 2 派工前訂定的判準跑第三輪**。該判準（`application-design:c4`：Major 若為上一輪修正引入則驗證輪不計入上限）的條件確實成立（Finding #2 為新引入 Major），但 `functional-design:c18` 的佔比訊號指向相反方向（新引入佔比 20%→40%，上升）。兩條規則衝突，由人工裁決停止並知情接受剩餘風險。決定與其理由寫進 `code-summary.md` 的「審查迴圈的停止決定」段。
- 2026-09-05T06:22Z — U-10a 新增 `run-probe-tests.py`（204 行）不在已核可計畫內（計畫 Step 5 逐字寫「本單元 `kind: packaging`，交付物是 YAML 設定，**沒有可單元測試的程式**」）。**該前提被本輪推翻**：probe 腳本是可抽出、可執行、可斷言的，只是需要以 `id: probe` 定位並用假 `git` 控制非 push 路徑。計畫本文不回改，以 code-summary 的兩處（「兩層驗證」與 Iteration 2 修正記錄）承接。
- 2026-09-05T14:52:48Z — U-6 停止於 reviewer iteration 2，**未跑第三輪**，而 lead 於派工前訂定的判準（`application-design:c4`）條件成立（iteration 2 的兩項 Major 以上分類皆為「新引入」）。人工決定為收掉。lead 的建議、雙方依據、以及「哪些部分只經過 lead 自己驗證」全部寫進 `code-summary.md` 的最末節交給 gate，不由 lead 片面選邊。
- 2026-09-05T14:52:48Z — U-6 的 `sync-state.json` **新增 `last_written_status` 欄位**，是本 intent 首次由下游單元擴充 U-4 擁有的 schema。**不需改動 U-4 的程式**（`record.sh` 以 `jq '. + $patch'` 合併並明文保留未知欄位），但 schema 的正式定義在 U-4，故仍屬跨單元事實。已列入待 gate 追認。
- 2026-09-05T17:10:13Z — U-7 依 C-7.1 修改了 R-6.1 的回寫欄位集合（五欄→四欄，移除 `last_synced_at`），這是**對已核可規則字面的偏離**，但依據是上游自己登錄的修法（`open-items.md:137` 逐字「R-6.2 的禁動欄位由一欄擴為兩欄」）且期限已過，故就地修而非再往後推一個 gate。連帶更新既有測試——它原本斷言的是**含 `last_synced_at` 的舊欄位集合**，等於把缺陷行為編碼進測試。
- 2026-09-05T17:10:13Z — U-7 撤回「C-2 是孤兒成員」的待追認項：**代號混淆**（C-2 是 `record-reader`、與 C-1 同一個 composite action，呼叫 `map.sh` 即呼叫它；`block.sh` 是 C-6，其**擁有單元**才是 U-2）。reconcile 的元件鏈從來就沒有 C-6，「不呼叫 `block.sh`」不構成缺口。**教訓：跨層引用元件代號前必須開 `components.md` 逐字比對，不得由檔名或單元編號推**。
- 2026-09-06T05:16:12Z — §13 學習儀式的候選數（51）遠超 AskUserQuestion 的每題 4 選項上限，依 `project.md` 的 `requirements-analysis:260822-ra-c2`（問題本身先收斂、記明合併方式，不在提問時臨時換一組）收斂為 4 項呈示：①守衛的「種類」錯了寫再細也沒用（c13）②一個欄位扛兩種語意就會裂開（c14）③交叉核對要追到條目「內部」的但書（c16／c17）④「未經實測」的否決等於還沒否決（c21）。收斂判準是「跨單元可複用的方法論教訓」，排除的 47 項為單一單元的實作事實，或已被 `project.md` 既有規則涵蓋（如停止判準須於該輪開始前訂定＝`functional-design:c18`、可算的數字先算＝`delivery-planning:dp-L1`、跨檔傳播＝`application-design:260822-ad-L1`／`units-generation:260822-ug-L1`）。另一項偏離：協定要求選項 `label` 逐字用候選 `summary`，但本 stage 的 summary 皆 200–400 字，超過 harness 對 label 的長度預期，故 label 用短標題、`description` 承載全文與可執行檢查。人工結果：四項全部未勾選，補充題答「沒有要補充的」，`persist` 回 `rule_learned:0`。（本條原以未查證的 `08:00Z` 寫入，違反 `project.md` 的 `user-stories:260822-us-L1`「寫時間戳前一律跑 `date -u`」；`date -u` 實測當時為 `2026-09-06T05:40:59Z`，本檔 mtime 為 `2026-09-06T05:16:12Z`，已更正為後者並記明此次違規。）

## Tradeoffs

- fixture runner（`run-fixtures.py`）的擁有者上游未指定——U-9 擁有 selftest workflow、U-1 擁有 fixture 集，中間的斷言器無人具名。定為 U-1 交付，理由是完成判準（「四條行為各有反例通過」）寫在 U-1；替代方案（歸 U-9、U-1 完成判準降為「fixture 集齊備」）在 Plan Approval 一併呈示，人工選擇前者。
- U-10a 的新機制下 CI run **仍會被建立**（顯示全部 Skipped），故 [US:S-1 AC 7] 的「不新增一輪四個 job」是以「無 job 執行」滿足，而非字面的「run 不存在」。原 `paths-ignore` 本可達成字面版本，但它在真正的失敗路徑上不生效——換掉的是一個假保證，不是降級。
- 2026-09-04T17:29:14Z — U-3 把欄位解析移出 R-2.4 視窗的代價：兩條 Aborted 分支中止前先付一次（分頁時多次）`ProjectV2.fields` 查詢；替代方案（維持舊順序、回頭修 `business-rules.md` R-2.4 對視窗量級的描述並讓 Bolt 1 gate 重新確認）會動已核可上游且讓已揭露的風險量級失真，故選前者。
- 2026-09-05T00:41:59Z — U-4 的 `hook declined` 歸為 `branch_protection`（不重試）是保守方向：自訂 hook 的非保護類拒絕會被冠上失真的 reason，但結果同樣是紅燈＋通報；另一邊（歸為可重試）會讓永久失敗被重試三次才紅燈。選前者。
- 2026-09-05T01:55:16Z — U-5 的 `LIST_LIMIT` 由計畫的 200 改為 500 並在命中上限時發警告：200 在通報 issue 累積後會**無聲**截斷，而無聲截斷在通報機制上等於漏報。代價是單次列舉的回應變大。
- 2026-09-05T02:32:22Z — U-5 的 Minor 2（換行切開的 token 逃逸遮罩）只**部分**可修：`single_line` 移前只救得了 `Authorization:` 那一類，token 被任意切割（空白、短前段）仍會逃逸。選擇把三條殘留形狀寫進程式碼註解並在測試中以**註解而非斷言**記載——把弱點斷言成「預期行為」會讓日後真要補強它的人看到紅燈。遮罩式清洗防的是不小心貼上，不是刻意規避。
- 2026-09-05T05:01:33Z — U-10a 的 `gate` job 讀不到 commit 訊息時往「不是同步」判（fail-open 到照跑 CI）：誤判成同步會讓一顆真正的開發者 commit 完全沒被檢查，誤判成非同步只是多跑一輪 CI。兩種誤判代價不對稱，故往後者倒並發 `::warning::`。
- 2026-09-05T06:22Z — U-10a 的 Finding #2（plan 與 summary 對「上游還欠什麼」給出不同答案）修法選擇**讓 plan 指向 summary 為單一來源**，而非把六列表格複製過去。複製會讓同一份清單有兩個副本、下次再漂移一次；根因是兩份文件都宣稱是正式來源，不是其中一份忘了更新。代價是讀 plan 的人必須再開一個檔。
- 2026-09-05T06:22Z — U-10a 的「不需要改的落點」清單由**枚舉**改為**判準**（歸屬／歷史類一律不需改）。起因是 reviewer 抓到枚舉漏了 `unit-of-work-dependency.md:76`。判準能讓下一個人自行判定新遇到的落點，枚舉不行——但代價是判準需要被正確理解，不像清單可以機械比對。
- 2026-09-05T14:52:48Z — U-6 的多輪收斂測試以「把上一輪回寫的部分物件合併進狀態檔、餵給下一輪」實作，而非讓 harness 支援真正的持久化。代價是測試自己要模擬合併語意（可能與 `record.sh` 的實際合併有落差）；換得的是零 harness 改動。reviewer 的 Minor 3 指出「harness 只模擬單輪」是那個 Critical 對 37 條綠燈隱形的結構原因，本測試關掉的是那個盲區而非整個 harness 的限制。
- 2026-09-05T17:10:13Z — U-7 對 reviewer 的第 4 項 Major（U-7 推送觸發 U-6 自我再觸發）**部分推翻而非照單接受**：`record.sh:367` 強制 commit 訊息含 `SYNC_MARKER`、U-7 的訊息含它、U-6 的防線②命中即整輪 skip——三者合起來使該 run **不會做 registry 全掃、也不會成為並行寫入者**。reviewer 對「run 會被建立」正確，對後果定性過重。殘留成本（每次對帳浪費一個立刻跳出的 run）降為 Minor 登錄。**接受 reviewer 的發現與接受它的定性是兩件事**。

## Open questions

- ~~**U-3 起，全部剩餘單元卡在 PRE-1**~~ — **已解除（2026-08-30T15:50:52Z，PRE-1 第五輪）**：`opendiamonds` token 已帶 `project` scope、測試看板 #23 建於同一擁有者名下，PRE-1 四項全部有答案。原文如下保留：**U-3 起，全部剩餘單元卡在 PRE-1**（憑證與 API 實測）。人工裁決為「先停，等憑證備妥」（2026-08-30T12:51:16Z）。清單已寫成可執行形式於 `<record>/construction/PRE-1-checklist.md`。U-3 的完成判準明訂「真實 Projects v2 API 讀寫，對獨立測試 Project」（ADR-A3），且本 repo 無 Projects v2 先例（實測 `.github/workflows/*.yml` 的 `graphql` **零命中**），查詢字串／錯誤碼／分頁游標全新寫——未經真實呼叫的這類程式碼信心很低，這是選擇停下而非照寫的理由。

- U-10a 的 `pull_request` 側 `if:` 判準未定：該事件取不到 `github.event.head_commit.message`，需改用 bot 身分，而實際值要到 PRE-1 鑄出憑證後才確定。已指派 code-generation 實作 `ci.yml` 時定案並實測。
- `business-rules.md`（U-1）有兩個 `R-6` 群（`scope_note` 推導、總函式性），同一識別字指兩件事。不使任何 AC 不可滿足，屬追溯風險；指派下次觸及該檔時把總函式性改編為 `R-7`。本 stage 一律以名稱稱呼，不用 `R-6`。

- ADR-A3 的「獨立測試 Project」限定條件本輪再加一項：**Status 選項須與正式看板同名**。測試看板 #23 的預設選項是 `Todo／In Progress／Done`，與 #16 的六個值完全不同——照原樣測，U-3 的映射邏輯會對著一組正式環境不存在的選項名被驗證通過。已以 `updateProjectV2Field` 對齊，但這是**組態上的補救，不是設計上的保證**；指派憑證策略 ADR 把它寫成 ADR-A3 的限定條件。
- 寫入身分由 GitHub App 改為擁有者 token 後，`repo` scope **整包**涵蓋 contents／issues／PR 寫入，**無 App 那種細緻權限可收斂**。合計 PRE-1-a 已確定的 ruleset 不可行，`requirements.md` OQ-1（把 repo 內容寫入權收斂到最小）的候選手段幾乎耗盡——這是憑證策略變更的**代價**，必須在 ADR 正面寫出，不得只寫「改用擁有者 token 即可」。
- Pull requests write 仍未實測（開 PR 會在 public repo 留下永久編號）。U-8 依賴它，但擁有者 token 的 `repo` scope 在機制上已涵蓋，資訊價值低於 App 時期；待人工裁決是否值得留下那個編號。

- **「已耗盡」「無可避免」「不可行」這類終局定性，寫下之前要把已經實測過的每一個事實各往反方向再問一次。** 推翻 ADR-0016 §7 的那個事實（本 repo 為 public）**第四輪就已實測記載**，當時只被記到「ruleset 不可行」那一側——同一個事實同時關掉一扇門、開啟另一扇，而只有關門那側被寫下來。這與既有的 `functional-design:c10`（規則生效前先推導狀態是否可達）同根但方向相反：那條防的是「宣稱能偵測到實際偵測不到的東西」，本條防的是「宣稱沒有路而實際有」。可執行檢查：任何終局定性都要能列出「我對哪些事實各問過正反兩次」。
- **PRE-1-c 為本輪新增的阻擋項**（`public_repo` ＋ `project` 的四條寫入路徑實測），未反映在 `bolt-plan.md` 的 PRE-1 表——該表現有六項。這是 ADR-0015 Context 點名過的同一個結構問題（在下游產出裡寫「指派」對已定稿上游是一張沒有收件人的便條），本輪已改由 ADR-0016 承載，但 `bolt-plan.md` 本身仍待修訂。

- **對持續被修訂的 artifact 使用 `檔名:行號` 引用，其失效是結構性的。** 本輪把 PRE-1-c 併進 `bolt-plan.md` 時，跨檔傳播檢查揭露該檔有約 20 處外部行號引用（散在 U-3／U-5／U-6／U-7 的 reviewer 查證表與 PRE-1 證據檔），而本輪的插入使 PRE-1 表之後整體下移。更要緊的是**其中部分在本輪之前就已失準**——多處以 `:51` 指稱「Bolt 1 的 Definition of Done」，該行現為 Bolt 序列表的一列——代表 ADR-0013／0014／0015 的歷次修訂已經讓它們漂移，只是沒人回頭掃過。處置：reviewer 查證記錄如實記載審查當時所見，**不回改**（回改等於竄改審查紀錄），改在 `bolt-plan.md` 加行號偏移告示並要求後續產出改用錨點引用。
- 本輪自身犯了兩個機械性錯誤，都是靠跨檔傳播檢查抓到而非事前避免：(1) 把 PRE-1-c 插在 PRE-1-b **之前**，使表格成為 a→c→b；(2) `pre-1-findings.md` → `PRE-1-results.md` 的更名**只改了兩處中的一處**。兩者都在「改動前先列出本輪要改動的每一個事實」那一步就該被列出——`application-design:260822-ad-L1` 與 `units-generation:260822-ug-L1` 都已要求，本輪照做了掃查卻沒把「新增列的**位置**」與「同一字串的**出現次數**」當成要列的事實。

- **一則錯誤引用在本 record 內連續傳播四次而零核對。** PRE-1 第三輪寫「U-3 `business-logic-model.md` GraphQL 使用 `organization(login:...)`，須改為 `user(login:...)`」，第四輪、第五輪、ADR-0016 §4.1 逐次沿用——實測 `grep -rn "organization(" U-3-board-client/` **零命中**，U-3 從未指定查詢根。**四次書寫都是從自己的前一份產出抄過來，沒有一次回到被引用的檔案。** 實質結論不變（查詢根必須是 `user`），但性質由「改為」變成「缺口待補」——照原文去找 `organization(login:)` 來改的人會找不到東西，並可能以為這一項已經有人做過。已更正 ADR §4.1 並在 PRE-1-results 補更正節。
- **不可達前提在本輪一次掃出三處**：U-3 的 R-4.2「組織政策阻擋」、[US:S-5 AC 2] 的同一子句、`requirements.md` 的 A-1 整條——全都因「無組織」而成為永遠走不到、卻看起來被涵蓋的分支。其中 **[US:S-5 AC 2] 特別值得記**：該 AC 的改寫理由逐字寫著「不改寫的話……會被實作成永遠走不到的死碼」，而**改寫後的版本自己又帶進一條不可達分支**。`functional-design:c10` 已附註「修正動作本身也要過這道檢查」，本例是它的第二次實證。
- **README 的看板連結匿名存取回 404**（新舊連結皆是）——Project #16 為 `public: false` 而本 repo 為 public。ADR-0016 §9 只授權修 URL 形狀（`/orgs/` → `/users/`），形狀已修正；但「public repo 的 README 宣告需求正本在一個外部讀者打不開的看板」是**另一個問題**，屬產品決定（把看板轉公開，或在 README 註明需授權），不在本 ADR 範圍，未逕自處置。
- U-4：GH006 的逐字文字未在真實 GitHub 上觀察（Plan Approval 裁決 4 刻意不推 `main`），stub fixture 的形狀來自文件；`main` 半邊的平台拒絕因此只有 stub 涵蓋。若未來要補 live 反例，前提是先有一個不會把機器 commit 落進 `main` 的安全做法，目前沒有。
- U-4 的 Major（U-7 推送落點在 U-4 自己的產出裡自相矛盾）以澄清處置：`R-7.2` 定義在 U-7 的 `business-rules.md`、衝突已由 ADR-0015 §13 的 Q6=A 收斂，但 U-4 `business-rules.md` 的待確認註記**未回改**（已核可上游）。只讀 U-4 的人仍會看到看似未決的開放項；指派 U-7 的 code-generation 明記結案、確認人 Bolt 2 gate。reviewer 找不到 R-7.2 是讀取範圍隔離的必然結果，不是它的疏漏——這也顯示 per-unit reviewer scope 對「跨單元契約引用」有結構性盲區，凡引用 sibling 單元的規則編號，都應在本單元產出內附上該規則的出處與內容摘要。
- U-5 實測推翻 `tech-stack-decisions.md` 的「改用 list --label 讀的是即時狀態而非索引」：`gh issue list --label` 與 REST 的 label 過濾列舉**都是最終一致的**（新 issue 約 4〜6 秒才出現）。產品行為不需改（ADR-A8 的 notify 自我收斂涵蓋它），但完成判準「連續兩輪」隱含一個未被寫下的前提：兩輪間隔必須大於該窗口。**未改上游**，落點與確認人（Bolt 1 gate）記在 U-5 的 code-summary。
- U-5 的 Major（批次鍵介面與 U-6 已核可 R-6.1a／R-6.1b 的「各呼叫一次」字面矛盾）以指派處置、不回改上游：指派 U-6 的 code-generation 以一個 step 帶全部鍵並在其 summary 明記收斂理由，確認人 Bolt 1 gate。**風險是沒有任何測試會發現它**——逐鍵呼叫在功能上是對的，只是貴（重現 FR-I4 要避免的每輪 30 次呼叫）。這是本 intent 第二次出現「per-unit reviewer scope 讓跨單元契約矛盾只能靠指派承接」的形狀（前一次是 U-4 的 R-7.2）。
- U-5 iteration 1 突變表的「M-A 紅 5 個斷言」reviewer 三次獨立重建得到 21／7／4，**未能複現該精確數字**。質性結論（改壞→紅、還原→乾淨）三次皆成立，但斷言數隨突變寫法浮動、本來就不是穩定量。教訓：突變表應記「哪些測試紅」與「質性結論」，斷言數只能標為該次觀測值，不得當成可複現常數——本 intent 的其餘突變表（U-1〜U-4）都寫了精確斷言數，同樣受此限制。
- U-10a 的 guard **尚未接進任何 workflow**，目前是一支要靠人記得手動跑的腳本（`grep -rn "check-ci-yml" .github/workflows/ .github/aw/` 只命中一行註解）。這是 Plan Approval 裁決項 2 的結果（接進 `repo-contract` 會變成 `ci.yml` 檢查自己——把 `if:` 改壞的那次修改同時讓檢查自己被 skip）。正確落點為 Bolt 1 整合驗證或另一支獨立 workflow，不在 U-10a 範圍。**「19 項全綠」不等於「這些設定受保護」。**
- U-10a 的真實完成判準（推一顆含 `[aidlc-sync]` 的 commit、觀察四道關卡有沒有跑）**尚未實測**，需真實 push，屬 Bolt 1 整合驗證。本單元目前只有靜態證據，沒有執行期證據。
- `[aidlc-sync]` 標記可被任何有推送權的人放進自己的 commit 訊息而整輪跳過四道關卡。`record.sh` 的 SEC-2 已記載同一風險，但 `gate` job 讓它多了一個生效面（PR 事件下 skip 全部四個 job），且本 repo 為 public。指派 Bolt 1 gate 判斷是否需要收斂。
- U-10a 的兩支驗證腳本（`check-ci-yml.py` 文字層、`run-probe-tests.py` 行為層）**都未接進任何 workflow**，目前都要靠人記得手動跑。這是 Plan Approval 裁決項 2 的延伸（接進 `ci.yml` 會變成自己檢查自己），但行為測試沒有這個循環問題——它測的是 probe 腳本的邏輯，不是 `ci.yml` 的形狀。**是否該把行為測試接進 CI（而文字檢查留在外面）未經評估**，指派 Bolt 1 gate。
- U-6 的兩欄分家修法、三條新測試（多輪收斂、R-6.1c 前提斷言、legacy 回退）與回退邏輯，**只經過 lead 自己的驗證，未被第三輪對抗式審查覆蓋**。lead 對它們的信心來自 lead 自己寫的測試——而「測試看起來在守、實際守不到」正是本 intent 已重複四次的失效形狀（U-10a 兩次、U-6 兩次）。gate 覆核時應優先看這一塊。
- U-6 的薄外層 `on: push`（無分支過濾）＋ 指向正式看板 #16，目前**只靠 `AIDLC_SYNC_TOKEN` 這個 secret 不存在**擋著（orchestrator 依 `project.md ## Mandated` 實地查證 secrets 與 variables 兩邊皆無）。且 #16 上目前**沒有** `AI-DLC Stage` 欄位，而 `write_field` 在欄位不存在時會自動建立——啟用時機與欄位名兩者必須由 gate 一併決定。
- `open-items.md` 的 **C-7.2** 在**實作面**已滿足（U-6 `forward-impl:707-710`），但它點名的**四處上游文件**仍帶修正前文字（`U-6/business-logic-model.md:53`／`:66`、`U-3/business-rules.md:104`、`U-3/domain-entities.md:50`）。**M-7.1** 的行為已覆蓋但序列圖未補。兩者的殘留都是上游 artifact，不由 code-generation 回改，**但 `open-items.md` 本身也沒有被更新為「實作面已關閉」**——gate 覆核時應同時處理這個記帳落差。

- **`ut` 沒有任何 required status check，`main` 唯一的一條在 skipped 時視同通過。** 實查（`gh api repos/opendiamonds/cloud-360/branches/{ut,main}/protection`）：`ut` 的 `required_status_checks` **整個 key 不存在**、`enforce_admins: false`；`main` 只有 `Repository contract`（即 `repo-contract` job 的 `name`），而 GitHub 官方文件逐字「Successful check statuses are success, **skipped**, and neutral」。合起來的後果是 U-10a 的 `gate` job 多了一個沒有被評估過的面：`[aidlc-sync]` 標記讓四道關卡全部 skip 時，**合併不會被擋**——`ut` 根本沒設 required check，而 `ut` 正是每個 Bolt 的合併目標、且 deploy-on-merge 掛在它上面。既有的 open item 只記到「標記可被任何有推送權的人放進 commit 訊息」，沒有記到「而且沒有任何東西擋著那次合併」。指派 Bolt 1 gate。
- **F5：三支 impl workflow 宣稱的「單一 intent 失敗不中止整輪」在真實 runner 上從未成立。** GitHub 對未指定 `shell:` 的 `run:` 用 `bash -e {0}`，而三支的註解都寫「`set -e` 刻意不開」、實作只有 `set -uo pipefail`——`set -<flags>` 只開不關。把三支測試 harness 改成忠實的 `bash -e` 之後，**三支共 23 條斷言變紅**（forward 3／reconcile 8／reverse 12），失敗形狀一致：第一個外部錯誤之後的 intent 呼叫次數為 **0**、通報數為 **0**。已加 `set +e`、改寫註解、各補一條釘住自己 `bash -e` 的行為迴歸測試。第四個實例在 `ci.yml` 的 `gate` job，已一併修（該處今天安全是巧合——每個可能回非 0 的命令剛好都在 `if` 條件位置）。
- **這是本 intent 第三次出現「跨單元的發現沒有傳播機制」。** U-9 自己的 `run-selftest-tests.py:1480-1489` 早就逐字記載了 `set -o` 只開不關的機制，但沒有人把它掃到三個兄弟單元。前兩次是 U-4 的 R-7.2 與 U-5 的批次鍵介面。per-unit reviewer 的視野結構性地停在單元邊界，而這類缺陷的**成因與後果分屬不同單元**。可執行的收斂方向（未實作，留給 gate）：任一單元查出「平台行為與我們的假設不同」時，強制對其餘單元的同型用法各 grep 一次並把結果寫進該單元的 summary。
- **`AIDLC_FORWARD_BASH` 這個環境變數名同時被兩支 harness 讀取，而兩者的正確預設值相反**：`run-orchestration-tests.py:69` 需要 `bash -e`（它跑的是從 impl workflow 抽出的腳本，CI 在 `-e` 下跑它），`run-live-tests.py:80` 需要不帶 `-e`（它直接 `bash <file>` 啟動 `.sh`，而巢狀的 `bash <file>` 不繼承父層 errexit）。目前靠 `IMPL_BASH` 另立一個變數繞開，但同名變數仍會同時影響兩者。只影響手動覆寫，未修。
- **`deploy.yml` 對反向 PR 完全沒有過濾。** 實查 `:10-14` 為 `on: pull_request: types: [closed], branches: [ut]`，無 `paths`。反向 PR 合併 ⇒ 觸發自架 runner 上 `timeout-minutes: 30` 的完整部署，為的是一個 JSON 欄位。**不在 U-10b 的擁有範圍**（其擁有欄逐字只寫「高成本 `on: pull_request` workflow」，指 PR 開啟側），且處置涉及 ADR-0008 的部署模型，未逕自擴大範圍。指派 gate。
- **U-10b 的 `aidlc-log.ts answer` 未能與寫檔在同一輪送出**（引擎回 `Refusing to record this answer: a real human has not acted at this checkpoint this turn.`）——一輪只容許一個人工檢查點，該輪已被 U-9 用掉。未繞過；事實寫進 U-10b 的問題檔檔頭，稽核事件待下一個真實人工回合補送。
- **U-9 的 R-1.2 掃描面在兩輪審查裡各被打穿一次，兩次的形狀相同：邊界由「檔案位置」界定而非「執行可達性」。** iteration 1 的邊界是 workflow 檔（把 agent 步驟藏進 composite action 或 `.sh` 即繞過）；iteration 2 修成「再看 `.github/actions/aidlc-sync-*/` 下的 `.sh`／`.py`」，reviewer 隨即以 **`python3 scripts/decide-status.py`** 繞過——而 `scripts/` 本來就是本 repo 的慣用落點（`ci.yml` 有三個呼叫點），**那不是繞過技巧，是照著既有形狀寫程式**。第三次改為執行可達閉包（遞迴解析 `python3 X`／`bash X`／`source X`／`./X`，解不開者 fail-closed），掃描面由 20 支腳本擴為 34 支、0 條解不開。**教訓不是「再多加一個目錄」而是「換原則」**：位置型邊界每一輪都會有下一格，總數不收斂。可執行檢查：任何「掃描集合」型的守衛，寫下之前先問「它是以位置界定還是以可達性界定」，前者要能說出下一格是什麼。
- **實作 line-continuation folding 之前，14 條 `bash "$X"` 呼叫全數漏掉，而檢查照樣綠。** 這是 U-9 iteration 2 實作者自己跑出來發現的，不是預測的。同批還有六項必須真的解開才不會誤判：`working-directory`（`ci.yml` 的 backend job 讓 `scripts/dump_openapi.py` 實指 `backend/scripts/dump_openapi.py`）、字面賦值鏈、**兩種** Actions 變數語法（`${GITHUB_ACTION_PATH}` 與 `${{ github.action_path }}`）、`$(cd … && pwd)` 慣用法、引號感知切分、函式內的包裝呼叫。**「解析腳本呼叫」這件事的實際難度遠高於它聽起來的樣子**——它比較像寫一個小型 shell 前端，而不是一次 grep。
- **兩條「當時是綠、實際什麼都沒驗」的測試由突變找出來**：`test_scripts_are_scanned_regardless_of_suffix` 只涵蓋「被呼叫的檔」（而可達閉包本來就不看副檔名），把它要守的修正退回去仍然綠；`test_the_checkers_own_behaviour_tests_run_in_ci` 用「該行不以 `#` 開頭」判斷，被 `run: echo '# … run-selftest-tests.py'` 打敗。**突變驗證的價值不只在「確認測試會紅」，更在「找出根本沒在守的測試」**——後者只有突變找得到，通過的測試看不出差別。
- **U-10b 的實作者在自己的修正裡發現同型缺陷並自行修掉**：絆線測試的第三條斷言原本從 `GH_AW_CARRIERS` 推導預期集合，而突變改的正是那個常數 ⇒ 兩邊一起縮水、測試通過。改為在測試內寫死 `EXPECTED_CARRIERS` 並就地註明「這一處刻意不從常數推導」。**這是本 intent 第一次由實作者自己的突變驗證抓到自己的假斷言**，而不是等 reviewer。
- **`test_the_real_repo_state_is_what_we_say_it_is` 這個「絆線」形狀值得保留為做法**：把「現在應該是紅的、而且恰好紅這幾項」寫成斷言，而不是寫在報告裡的一句話。U-10b 交付時它如預期紅了，逼實作者回來更新預期集合——若只寫在報告裡，那句話會靜默過期。
- **U-10b 的 lock 可重現性是整個交付的基礎，且必須在真實 repo 上證一次。** 用釘住的 v0.81.6 對**未修改**的四支重編，四支 lock 逐位元相同（md5 不變、`git status` 無異動）；改 `.md` 後重編，每檔 diff 恰 **4 行**（`frontmatter_hash` 換掉 ＋ 兩行 `paths-ignore`），`body_hash`／`compiler_version`／整個 `gh-aw-manifest` 逐位元不變 ⇒ **機械證明沒有夾帶供應鏈升級**。對照組：本機 v0.86.2 每檔 526 行（action SHA、防火牆容器、mcpg、github-mcp-server 全升）。
- **`COMPILED:` 檢查的是 glob 一致性，不是 `compiler_version`** ⇒ 沒有任何機制擋得住有人用較新的 gh-aw 重編。已寫進絆線的紅燈訊息，但**未強制**。指派 gate。
- **本 intent 第三次「可算的數字沒算就寫」**：U-9 iteration 2 實作者回報 `run-selftest-tests.py` 為 2226 行，實際 2248。前兩次是 U-8 的 1231 vs 1233、U-9 的「39 條 map 測試」vs 實際 38。三次都不影響正確性，但三次都出現在**同一種位置**——報告裡用來支撐論述的數字。`delivery-planning:dp-L1` 已要求「寫下去之前先算」，本 intent 仍犯三次，代表該規則的可執行性不足：它沒說「報告也算 artifact」。
