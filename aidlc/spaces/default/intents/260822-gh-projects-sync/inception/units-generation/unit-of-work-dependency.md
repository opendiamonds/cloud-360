# Unit of Work Dependency — AI-DLC 與 GitHub Projects 的進度同步

<!-- Stage: units-generation（Inception 2.7）· Record: 260822-gh-projects-sync
     **本檔只描述拓撲（誰可以依賴誰），不挑實作順序、不指出關鍵路徑**——那是 2.8
     delivery-planning 用這張圖做的經濟決策。單元定義見 unit-of-work.md，
     上游輸入清單見該檔 §上游輸入（application-design 五份、requirements、stories）。 -->

## 依賴的定義

本檔的一條邊 `A → B`（A 依賴 B）意思是：**A 的完成判準在 B 不存在時無法被驗證**。判準包含驗證——依 `project.md` 的切分判準，一個單元「完成了嗎」的答案必須包含它是否被驗過。

**不是邊的東西**：經濟性的先後（先做哪個比較划算）、避免重工的建議順序、以及**同批次約束**——後者另列於下方獨立表格，因為 edge block 的 `depends_on` 放不下第二種關係。

## 邊清單（散文）

| 單元 | 依賴 | 為什麼 |
| --- | --- | --- |
| U-1 映射與解析 | （無） | 純函式 ＋ 自帶 fixture，零外部相依 |
| U-2 受管區塊 | （無） | 同上 |
| U-3 看板客戶端 | （無） | 其驗證需要一個測試看板，但那是 config 與 PRE-1 的實測項，不是單元 |
| U-4 record 回寫 | （無） | 其驗證需要分支保護設定，同樣是 config 而非單元 |
| U-5 通報 | （無） | 直接呼叫 Issues REST，不經其他單元（[ad:component-dependency.md] 已裁定 C-5 不經 C-3） |
| U-6 正向同步 workflow | U-1、U-2、U-3、U-4、U-5 | 它是這五者的編排者；缺任一個都跑不出一次完整的正向同步 |
| U-7 對帳 workflow | U-1、U-2、U-3、U-4、U-5 | 同上；`ReconcileReport` 的每個欄位都來自這五者之一 |
| U-8 反向同步 workflow | U-2、U-3、U-4、U-5 | 需要雜湊比對（U-2）、讀看板（U-3）、寫檔開 PR（U-4）、通報（U-5）。**不需要 U-1**——反向路徑不做 Status 映射 |
| U-9 自我測試 workflow | U-1、U-3、U-6、U-7、U-8 | AC 1 以 fixture 驅動 U-1；AC 2 端到端需 U-3；AC 4 的靜態檢查要檢視 workflow 的 job 定義，故需三支 workflow 存在 |
| U-10a `ci.yml` 回寫排除 | U-4 | 其完成判準是「回寫 commit 不取消既有 CI run」，需 U-4 產生回寫 commit 才觀察得到 |
| U-10b 反向 PR workflow 排除 | U-8 | 其完成判準是「反向 PR 不觸發 `ui-regression`」，需 U-8 產生反向 PR 才觀察得到 |
| U-11 README 指路 | （無） | 純文字新增 |

## 機器可讀的邊 block

下游的批次 fan-out 由此 block 計算，不由上方散文計算。

```yaml
units:
  - name: U-1-map-parse-action
    kind: library
    depends_on: []
  - name: U-2-managed-block
    kind: library
    depends_on: []
  - name: U-3-board-client
    kind: library
    depends_on: []
  - name: U-4-binding-store
    kind: library
    depends_on: []
  - name: U-5-notifier
    kind: library
    depends_on: []
  - name: U-6-forward-workflow
    kind: service
    depends_on: [U-1-map-parse-action, U-2-managed-block, U-3-board-client, U-4-binding-store, U-5-notifier]
  - name: U-7-reconcile-workflow
    kind: service
    depends_on: [U-1-map-parse-action, U-2-managed-block, U-3-board-client, U-4-binding-store, U-5-notifier]
  - name: U-8-reverse-workflow
    kind: service
    depends_on: [U-2-managed-block, U-3-board-client, U-4-binding-store, U-5-notifier]
  - name: U-9-selftest-workflow
    kind: service
    depends_on: [U-1-map-parse-action, U-3-board-client, U-6-forward-workflow, U-7-reconcile-workflow, U-8-reverse-workflow]
  - name: U-10a-ci-writeback-exclusion
    kind: packaging
    depends_on: [U-4-binding-store]
  - name: U-10b-reverse-pr-workflow-exclusion
    kind: packaging
    depends_on: [U-8-reverse-workflow]
  - name: U-11-readme-pointer
    depends_on: []
```

**無環**：五個葉節點（U-1～U-5）與 U-11 無出邊；U-6／U-7／U-8 只指向葉節點；U-9 指向葉節點與三支 workflow；U-10a 指向 U-4、U-10b 指向 U-8。無任何路徑回到起點。

**曾經有環、已消除**：rev0 初稿把 [US:S-1 AC 7]（回寫 commit 不得取消既有 `ci.yml` run）同時掛在 U-4 與當時尚未拆分的 U-10 的完成判準上，形成 `U-4 → U-10 → U-4`。已改為**只歸 U-10**——讓那件事為真的機制是 `ci.yml` 的 `paths-ignore`，不是 U-4 的回寫行為。

## 整合點與契約

| 邊 | 介面 | 破壞性變更的影響 |
| --- | --- | --- |
| U-6／U-7 → U-1 | `map(ParsedRecord, Config) -> Decision`；`Decision` 含 `status \| null`、`field_value`、`reason_code`、`traceable_row` | 新增 `reason_code` 值時，所有消費端的分支必須有 default，不得靜默落入「照寫」 |
| U-6／U-7／U-8 → U-4 | `<record>/sync-state.json` 的 schema | **跨輪相容性必須維持**——舊格式在新版讀取時不得崩潰；schema 需含版本欄位 |
| **U-6／U-7／U-8** → U-2 | 受管區塊的標記與 `content_hash` | **改格式會使全部既有 item 的雜湊失效**，下一輪反向同步把全部 item 誤判為人為變更（[ad:ADR-A6]，本設計最不易反轉的一項） |
| U-6／U-7／U-8 → U-5 | `FailureIdentity = (intent_id, reason_code)`；通報 issue 的標題慣例與 label | 改變慣例會讓既有開啟中 issue 找不到，退化為每輪開新 issue |
| U-6／U-7／U-8 → U-3 | 六個方法簽章；`WriteResult = Written \| Aborted \| Failed` | `Aborted` 若被改成拋例外，會讓「回讀不符」從正常判斷變成紅燈，違反 [US:S-8 AC 1] 的適用前提 |
| 全部 `service` 單元 → Config | `project_number`、`project_owner`、`record_root`、`stage_field_name`、`whitelist`、`reconcile_batch_size`；secrets `app_id`／`app_private_key` | 改 input 名稱等同破壞可重用性（[ad:ADR-A10]），呼叫端的薄外層要同步改 |

## 平行開發機會

下列各組**彼此之間沒有依賴邊**，多個拓撲排序都成立。本節只陳述拓撲事實，**不建議先做哪一組**：

- **{U-1, U-2, U-3, U-4, U-5, U-11}** — 六個單元互相之間零依賴邊。
- **{U-6, U-7}** — 依賴集合相同，彼此無邊。
- **{U-8}** 與 **{U-6, U-7}** — 彼此無邊（但見下方同批次約束）。
- **{U-10a}** 與 **{U-10b}** — 兩者無邊，且消費端不同（U-4 vs U-8）。
- **{U-11}** 與其餘全部 — 完全獨立。

## 同批次約束（**不是** DAG 邊）

依 [Q3=A]：技術上各單元可獨立部署，但下列組合**不得分批進入 `ut`**。它們承接 `stories.md` 已定案的三處（G1／G2／G3），性質是「中間態會讓看板說謊」而非「技術上做不到」。

**在 DAG 上長得像依賴邊，但不是**——依賴邊說的是「沒有 B 就驗不了 A」，同批次約束說的是「A 和 B 可以分別驗，但不能分別上線」。edge block 的 `depends_on` 表達不了後者，故列於此。

| 不得分批的組合 | 承接自 | 中間態會發生什麼 |
| --- | --- | --- |
| **U-6 ＋ U-1、U-2、U-3、U-4、U-5** | `stories.md` G1（S-2↔S-3）、G3（S-1 不得單獨上線） | U-6 單獨上線 = 機制開始寫看板但沒有寫入前回讀、沒有分岔通報、沒有無法解析就跳過。「寧可不寫，不可寫錯」的取捨被倒過來。另：只有 U-6 而缺其餘，卡片會停在 `Ready` 不動——對 P3 那是一格謊 |
| **U-8 ＋ U-6** | `stories.md` G2（S-6↔S-2 的 FR-G3 分支） | U-8 先上而 U-6 尚無暫停覆寫分支 ⇒ 反向 PR 開啟的整段期間，正向同步會把協作者在看板上的改動輾回去——正是 U-8 存在的唯一理由 |
| **U-10a ＋ U-4** | [US:S-1 AC 7] | U-4 先上而 `ci.yml` 未加 `paths-ignore` ⇒ 每次回寫都取消開發者當下的 CI run。**這一組同時也是依賴邊**（U-10a → U-4），兩種關係在此重合，但成因不同：依賴是「沒有 U-4 就沒有 commit 可測」，同批次是「有 U-4 而沒有 U-10a 會弄壞別人的 CI」 |
| **U-10b ＋ U-8** | [US:S-6 AC 7] | U-8 先上而高成本 workflow 未排除 ⇒ 每天把只改同步狀態檔的 diff 送進含 6 次 LLM agent 執行的完整 gauntlet。同樣與依賴邊重合（U-10b → U-8） |

**U-9 不在任何同批次約束內**：它是驗證層，晚於被測對象上線不會讓任何人看到錯的東西。

## 對 2.8 的交付

本檔提供的是**拓撲與約束**：**12 個節點**、**21 條依賴邊**（U-6 五條、U-7 五條、U-8 四條、U-9 五條、U-10a 一條、U-10b 一條；U-1～U-5 與 U-11 無出邊）、**4 組同批次約束**、5 組可平行開發的集合。

**本檔刻意不提供**：建議的 Bolt 序列、關鍵路徑、哪個單元先做比較划算、以及任何形式的「第一批應該包含什麼」。那些是 2.8 用這張圖做的經濟決策，需要人對「先證明什麼」的價值判斷。

## Review

**Reviewer:** aidlc-architecture-reviewer-agent
**Iteration:** 1（Revision 1 重審）
**Date:** 2026-08-29T03:49:39Z
**Verdict:** READY

> 本輪審的是 **Revision 1**（U-10 拆為 U-10a／U-10b，觸發自 delivery-planning [dp:F1=A]），不是重新審整份 2.7。iteration 2 已判 READY 的 rev0 內容（G-1 契約缺口的處置、S-2/S-3 co-ownership 等）除非被本次修訂觸及，一律不重新開啟；本節取代 iteration 2 的整段舊 Review，因為它描述的是 rev0，已被 Modify 跳轉取代。逐行 diff `archive/*.rev0.md` 對三份現行產出確認：本輪唯一改動的是 U-10 相關內容（`unit-of-work.md` 兩處小段落＋ U-10 節拆分＋新增 Revision 1 段；`unit-of-work-dependency.md` 的邊表／yaml／同批次表／平行機會／交付數字；`unit-of-work-story-map.md` 的 6 處 U-10 引用），其餘逐字未動——與 Revision 1 段自述的改動範圍相符。

### 對拆分正當性的判斷

**這是對 2.7 自己判準的忠實重新套用，不是為了閃避排程僵局而發明的事後理由**——雖然觸發它的確實是 2.8 的排程僵局，但正當性本身站得住，理由如下：

1. **引用的判準逐字為真，不是曲解**。`project.md ## Corrections` 的 `cid:units-generation:c6` 原文是「工作單元的切分判準是「驗證方式**與失敗模式**是否同類」」——本輪 Revision 1 段引用的正是這句，且「與失敗模式」四字確實在原文裡，不是本輪臨時加碼的新標準。
2. **rev0 自己的邊表早就寫出兩個不同消費端，只是沒有據此拆分**。`archive/unit-of-work-dependency.rev0.md:27` 的 U-10 一列寫著「其完成判準是「回寫 commit 不取消既有 CI run」與「反向 PR 不觸發 `ui-regression`」——**兩者都需要有東西可觀察**：前者需 U-4 產生回寫 commit，後者需 U-8 產生反向 PR」——消費端不同這件事在 rev0 就已經白紙黑字，本輪不是發現新事實，是把既有事實正確地轉譯成單元邊界決定。這比「臨時找理由」的假設更弱：證據早就在文件裡。
3. **同一份文件內有直接先例，且先例做的是同一件事**。`unit-of-work.md:34`（`第⑥類拆成四個單元而非一個`）用的正是「失敗模式不同」把一個驗證方式類別（⑥workflow 執行期）拆成 U-6～U-9 四個單元。U-10a／U-10b 的拆分是對「建置與觸發設定」這個第七類（不在六類驗證方式內，見 `unit-of-work.md:32-33` 的新註）套用**完全相同的邏輯**，不是不同標準。
4. **拆分帶來真實的完成判準簡化，不只是換名字**。逐字核對兩個新單元的「完成判準」：U-10a（`unit-of-work.md:154`）只讀「回寫 commit 推送後，該分支上既有的 `ci.yml` run 未被取消，且未新增一輪四個 job」——不再提 U-8 或反向 PR；U-10b（`:167`）只讀「反向 PR 開啟後，`ui-regression` 未對其執行」——不再提 U-4 或回寫 commit。對照 yaml block：U-10a 的 `depends_on` 從 rev0 的 `[U-4, U-8]` 縮成 `[U-4]`；U-10b 縮成 `[U-8]`（已用腳本重新解析確認，見下方「逐項驗證」第 2 點）。**這是可驗證的依賴減少，不是文字重排**。
5. **與 C-1／C-2 合併進 U-1 的既有決定對照，拆分方向也一致**。`units-generation-questions.md` Q1 選項 B 拒絕拆開 C-1／C-2 的理由是「共用一個 composite action、共用一套 fixture、共用一次部署」——即physically共享同一個交付物。U-10（拆分前）的「交付」欄寫的是「對 `.github/workflows/` 下**既有**檔案的修改」，實際是對**兩個不同檔案**（`ci.yml` 與高成本 workflow）的修改，並不共享單一交付物——這代表 rev0 把 U-10 併成一個單元的理由，原本就比 C-1／C-2 合併的理由弱，拆分並不違反本站在同一份問題檔裡已經用過的「共享交付物才合併」邏輯，反而更貼合它。

**結論**：正當性成立，不是包裝過的排程妥協。這不代表沒有代價——見「逐項驗證」第 1 點對 Bolt 1 仍為 7 個單元的分析。

### 逐項驗證

**1. 拆分是否真的解掉了 delivery-planning 的問題？—— 是，已重新推導出一條可行序列。**

以現行（拆分後）的 DAG 邊、4 條同批次約束（`unit-of-work-dependency.md:102-110`）與 [dp:Q1=C] 定案的排序讀法（U-4＋U-10a／U-8＋U-10b 為真捆綁，U-6 須早於 U-8 為方向性排序）手動推導：

| Bolt | 內容 | 單元數 | 滿足的約束 |
| --- | --- | --- | --- |
| 1 | U-1、U-2、U-3、U-4、U-5、U-6、U-10a | 7 | U-6＋U-1～U-5 同批（row 1）；U-10a＋U-4 同批（row 3） |
| 2 | U-7 | 1 | 僅需 U-1～U-5，已滿足；無同批次約束 |
| 3 | U-8、U-10b | 2 | U-6 已上線（row 2 的排序）；U-10b＋U-8 同批（row 4） |
| 4 | U-9 | 1 | 需 U-1、U-3、U-6、U-7、U-8，Bolt 1–3 已全數上線 |
| （任意時點） | U-11 | 1 | 無約束 |

四項驗證全部通過：(a) 每條 `depends_on` 邊都在消費者所在 Bolt 之前或同批已滿足；(b) U-4＋U-10a 與 U-8＋U-10b 各自同批；(c) U-6（Bolt 1）早於 U-8（Bolt 3）；(d) 最大 Bolt 為 7，不含 8 單元巨型 Bolt。**拆分前**（rev0）的等價推導會在 U-10 進 Bolt 1 時被迫拉入 U-8（因 rev0 的 U-10 `depends_on: [U-4, U-8]`），而 U-8 又拉入 U-6 的排序前提，最終逼出 [dp:F1] 描述的 8 單元批次——這條路徑在拆分後**不再存在**，因為 U-10a 已不再依賴 U-8。**拆分確實達成了它被交付的目的**，不是換皮不換骨。

**Bolt 1 為何仍是 7 個單元，是否可以更小？** 這 7 個單元被迫同批，來自兩條**未被本輪修訂觸及**的既有約束：row 1（U-6＋U-1～U-5，來自 `stories.md` G1／G3，rev0 起即有，iteration 2 已核可未動）與 row 3（U-10a＋U-4，本輪拆分的直接後果，但其必要性本身不是本輪新增——U-6 是這五個 library 的編排者，其 workflow 若在它們尚未存在於 repo 內時就合併，會在下一次觸發時直接引用不存在的 composite action 而失敗，是真實的技術先後要求，不只是經濟排序）。**這不是本輪修訂造成的問題**，也不在 Revision 1 宣告的改動範圍內（「僅 U-10 相關」），故不計入本次的 NOT-READY 判準；但值得記錄供 2.8 參考：拆分把逼出巨型批次的**成因從「U-10 的雙重依賴」單一化為「row 1 的既有捆綁要求」**，Bolt 1 的 7 單元規模是後者單獨造成，並非拆分的副作用或拆分不徹底。

**2. yaml edge block 機器複驗**——以腳本重新解析（非沿用上一輪結果）：12 節點、21 條邊、DFS 確認**無環**、`depends_on` 目標**無懸空**、`kind` 全為合法值（`library`×5、`service`×4、`packaging`×2、`U-11` 留空——此為 rev0 起即有的既存狀態，本輪未變動）。逐邊核對 U-10a（`depends_on: [U-4-binding-store]`）與 U-10b（`depends_on: [U-8-reverse-workflow]`），與散文邊表（`unit-of-work-dependency.md:27-28`）、同批次表（`:109-110`）、平行機會表新增列（`:96`）逐字一致。

**3. 跨檔傳播**——已對三份現行產出與 rev0 archive 逐行 diff（見上方引言段的方法說明），確認：
   - `unit-of-work.md` 的 U-10 段正確拆為 `### U-10a` 與 `### U-10b`（`:148`、`:161`），U-4 的「不擁有」列（`:83`）改指向 U-10a，六類驗證方式表下新增 U-10a／U-10b 說明（`:32-33`）。
   - `unit-of-work-dependency.md` 的散文邊表、yaml block、無環說明、平行機會、同批次表、交付數字（12 節點／21 邊／4 組同批次／5 組平行）全數同步，逐項見「逐項驗證」第 2 點。
   - `unit-of-work-story-map.md` 的「故事 → 單元對照」（S-1 AC 7 → U-10a，`:20`；S-6 AC 7 → U-10b，`:38`）、「跨單元的故事」表（S-1、S-6 兩列，`:51`、`:55`）、「單元內的 AC 先後」表的「其餘」列（`:68`）、「每個單元都有故事」覆蓋表（拆為 U-10a／U-10b 兩列，`:89-90`）全數同步。
   - **iteration 2 遺留的 Major（S-2／S-3 的 cross-cutting 表漏列 U-7）已在 rev0 存檔中即為已修復狀態**（`archive/unit-of-work-story-map.rev0.md:51-52` 已含 U-7）——即這項修復發生在 rev0 定案之後、本輪拆分之前，不是本輪的功勞也不是本輪的責任，予以確認並跳過，不重新列入本輪計分。
   - **本輪新引入兩處未同步的殘留**，兩者都是同一種失敗模式（衍生統計句未隨結構變更更新），見下方「本輪新發現」。

**4. derived numbers**——單元數 11→12（正確，於「## 單元定義」的 12 個 `###` 標頭與 yaml 的 12 節點兩處都對）；邊數維持 21（U-10 原本 2 條出邊〔→U-4、→U-8〕拆給 U-10a 1 條、U-10b 1 條，總數不變，機械上合理）；同批次約束 3→4（U-10＋U-4 一條拆為 U-10a＋U-4、U-10b＋U-8 兩條，凈增 1，見 `:109-110`）；平行機會集合 4→5（新增「{U-10a} 與 {U-10b}」一組，`:96`）。四項數字都與腳本複驗結果一致。**但**兩處由這次結構變更「應該」連動更新、實際未更新的衍生敘述句，見下方「本輪新發現」。

覆蓋claim（11 故事／65 AC／每單元皆有故事）：逐條重算 S-1（7 AC：AC1→U-3＋U-6、AC2-3-4→U-4、AC5→U-6、AC6→U-3、AC7→U-10a）與 S-6（7 AC：AC1-5→U-8、AC6→U-2、AC7→U-10b），1–7 各出現恰好一次、無重複無遺漏，AC 層級的拆分是乾淨的機械換名（label 從 U-10 改為 U-10a／U-10b），未動任何 AC 歸屬的實質判斷。「每個單元都有故事」覆蓋表 12 列（U-1～U-9、U-10a、U-10b、U-11）每列至少一則故事，無空單元——**表格本身正確**，但其後的摘要句仍寫「11 個單元」，見下方。

**5. Q1～Q4 與 Step 5 計畫不重取，是否合理？—— 合理，但有一點需要向人明確揭露。** 逐項核對四題實質是否被拆分改變：[Q1=A]（依驗證方式切）——U-10a／U-10b 仍歸類在「建置與觸發設定」這個第七類，切分軸本身未變，只是同一軸內切得更細，與 [Q1=A] 已接受的「六類之外另有收尾項」框架一致；[Q2=A]（8–10 個，允許超出並記載理由）——本輪超出的量從 1 個變 2 個，理由的**性質**未變（仍是「同一驗證方式類別內失敗模式不同故再拆」），只是新增了一個獨立於原「四支 workflow」理由之外的第二個超出來源；[Q3=A]（技術上獨立部署＋同批次約束另標）——機制不變，U-10a／U-10b 的同批次約束用的正是原本就存在的表格形式；[Q4=A]（kind 標註原則）——U-10a／U-10b 均維持 `packaging`，與「既有檔案調整標 packaging」的原則逐字一致。四題實質均未被拆分觸及，不重問站得住腳。**但**：Step 5 核可的原始計畫表（`units-generation-questions.md:130`）字面上是**11 列、U-10 為其中一列**，這是使用者當時實際按下核可的具體清單；現行決定是 12 個單元。`unit-of-work.md:208` 的 Revision 1 段誠實揭露了「不重取」這件事並說明理由，**沒有**宣稱 12 單元的新清單已被核可——這符合 `project.md ## Corrections` 的 `cid:approval-handoff:260823-rev1-c1`（不得把舊確認寫成涵蓋新清單）的字面要求，因為它根本沒有那樣宣稱。是否需要為 12 單元的具體清單另外取得一次人工核可，是本站 gate 流程本身要處理的事（本次 Modify 跳轉之後、進入下一步前應有的核可關卡），不是這份 artifact 內部自相矛盾的問題——**判定為合理揭露，非違規**，但提醒 gate 環節：若後續核可只問「同不同意 reviewer 判定」而不呈現「Step 5 表仍是 11 列」這件事實，使用者可能誤以為 12 單元已在原始 Step 5 核可範圍內。

**6. U-10a／U-10b 各自只帶一則故事的一條 AC，是否湊不出信心假說？—— 不構成問題。** `project.md` 的「湊不出信心假說」判準（`cid:delivery-planning:c3`）針對的是 **Bolt**（可展示、可部署的交付批次），不是**單元**本身；單元本來就是被組裝進 Bolt 的更小顆粒。同批次約束表已保證 U-10a 永遠與 U-4（及其連帶的 U-1～U-6，見上方 Bolt 1 推導）同批、U-10b 永遠與 U-8 同批——兩者都**不可能**單獨構成一個 Bolt，故其個別故事覆蓋率低並不違反判準的實際意圖。

**7. 邊界檢查——是否夾帶了實作順序或關鍵路徑語言？—— 未夾帶。** 全文 grep「Bolt」「先做」「先上線」「建議順序」「關鍵路徑」「順序」，命中處逐一核對：`unit-of-work.md:204` 與 `:206`（Revision 1 段落）描述的是「2.8 做了什麼、發現了什麼」——即敘述觸發本次修訂的外部事件，屬史實記載而非本站對實作順序的建議；其餘命中全部是既有的免責聲明句（如「不含實作順序或關鍵路徑」）或既有的資料相依先後說明（`unit-of-work-story-map.md:59` 的「單元內 AC 先後」，屬單元內部資料相依而非跨單元排序，且非本輪新增段落）。**乾淨。**

### 本輪新發現

**Major #1 — `unit-of-work.md:34` 的衍生統計句未隨拆分更新，且其「唯一原因」的字面宣稱現已為假。**

原句：「這是 **11 個單元**略高於 [Q2=A] 的 8–10 的**唯一原因**，如實記載而非硬併。」——就在同一區塊的**上兩行**（`:32-33`），本輪已新增 U-10a／U-10b 拆分的說明；`:34` 這句話本身描述的是「為何超出 8–10」，理應在同一次修訂中同步更新，卻仍停在拆分前的計數與歸因。兩個具體問題：(a) 單位數應為 12，不是 11；(b) 「唯一原因」在拆分後已不成立——U-10 拆分本身就是超出範圍的**第二個**獨立來源（如 `units-generation-questions.md:220` 的 Revision 1 段自己所述：「單元數 11 → 12...拆分後多一個」），與 `:34` 自稱「唯一」直接矛盾。**這不會誤導實作**——「## 單元定義」的 12 個 `###` 標頭本身正確且無歧義，一個依那些標頭施工的開發者不會被這句衍生說明文字帶偏——但這是一個真實、可核對、由本輪修訂自己造成的內部矛盾，且與 `project.md ## Corrections` 已記載至少三次的同型失誤（`cid:application-design:260822-ad-L1`、`cid:units-generation:c6b`、`cid:units-generation:260822-ug-L1`）屬同一種模式：改動一個結構性事實時，只改了它的第一個出現位置，沒有查找其餘引用該數字的地方。修法機械：把 `:34` 改為「12 個單元」並拿掉或改寫「唯一原因」（現在有兩個獨立原因：四支 workflow 的失敗模式互異、以及 U-10 依失敗模式二次拆分）。

**Major #2 — `unit-of-work-story-map.md:93` 的「無空單元」摘要句同型未同步，仍寫「11 個單元」。**

原句：「**無空單元**——11 個單元每個至少承載一則故事的 AC。」——它**緊接在**已正確拆列為 12 列（U-1～U-9、U-10a、U-10b、U-11，`:76-89`）的「每個單元都有故事」覆蓋表之後，是對那張表的摘要陳述，但摘要用的計數沒有跟著表格一起更新。與 Major #1 同一種缺陷形狀（權威表格正確、其後的衍生摘要句未同步），嚴重度理由也相同：表格本身無歧義，讀表格的人不會被誤導，但這句話單獨讀會與其正上方的表格內容矛盾。修法機械：把「11」改為「12」。

**判定**：0 Critical、2 Major——均未超過「>2 Major 才 NOT-READY」的門檻，**verdict 維持 READY**，但這兩項是本輪修訂自己造成的、機械可修的已知缺陷，不是可以擱置的建議，也不應以「範圍僅限 U-10」為由排除在外——它們就是 U-10 拆分這個改動本身遺漏的收尾。連同 iteration 2 已指出、本 intent 至今第三／四次重演的「衍生數字未同步」模式，這已經是可觀察的重複失效模式，建議在下一輪 §13 Learnings 或 practices-discovery 提升為更具體、更早介入的機械檢查（例如：任何提及單元總數的句子在 commit 前先以正則表達式抓出並列成清單逐一核對），而不只是仰賴修訂後的人工 grep。

**Minor（觀察，非缺陷）**——Revision 1 段（`unit-of-work.md:212`）的改動摘要表寫「總覽表拆列」，但 `unit-of-work.md` 本身沒有一張字面上叫「總覽表」的表格；比對後判斷這句話實際指的是「## 單元定義」下 U-10 的 `###` 小節拆分（確認拆分本身正確，見「逐項驗證」第 3 點），用詞不夠精確但沒有造成事實錯誤，不計入 Major。

### Attempted refutations that did not hold

- **嘗試論證「failure-mode 差異」只是包裝過的排程妥協、拆分本身站不住腳**：被推翻——見「對拆分正當性的判斷」第 1–5 點：引用的判準逐字為真、rev0 自己的邊表早有消費端分歧的證據、同一份文件內有直接先例（第⑥類拆四單元）、完成判準確實變窄（依賴減少可用腳本驗證，非文字重排）、且與既有的 C-1／C-2 合併判準方向一致。四條獨立證據線同時成立，不成立「純粹是為了閃避 8 單元批次才發明的理由」這個假設。
- **嘗試論證 Bolt 1 仍是 7 個單元代表拆分沒有真正解決問題**：被推翻——見「逐項驗證」第 1 點，已用完整推導證明一條合法序列存在（DAG、同批次、排序、規模上限四項皆滿足），且拆分把原本會被拉進 Bolt 1 的 U-8（連帶其整個反向同步複雜度）完全移出，只是把批次從 8 縮到 7；殘留的 7 這個數字可歸因到兩條**未被本輪觸及**的既有約束（row 1 的 U-6 捆綁、U-10a 對 U-4 的必然捆綁），不是拆分本身不徹底。
- **嘗試在 `unit-of-work.md`／`unit-of-work-dependency.md` 全文尋找除了 Major #1、#2 之外的其他殘留 `U-10`（未加 a/b）引用**：查無——已用 grep 排除法逐一核對所有 bare `U-10` 命中，其餘全部落在明確的歷史敘述語境（rev0 archive 比較、「曾經有環、已消除」說明、Revision 1 觸發原因段），符合 `project.md` 對「刻意保留的歷史 vs 過期殘留」的區分標準，判定為正確的歷史記載而非新問題。
- **嘗試論證整合點契約表（`unit-of-work-dependency.md` 的「整合點與契約」表）漏列 U-10a／U-10b**：查證後不成立——該表在 rev0 與現行版本中都**完全不提 U-10**（其欄位是 C-1／C-4／C-6／C-5／C-3／Config 這些跨單元共用介面，U-10 兩半都是純建置期設定、不對外暴露方法簽章或型別契約），故無需在此表新增列，Revision 1 段所稱「整合契約...交付數字同步」應理解為「確認不需要改」而非「已改」，非缺陷。
